---
type: doc
title: "Evidence: adversarial review of the draft"
description: "A second model argued against every non-keep row, stance, new step and finding of the step review, with evidence, and said agree where it agreed; the source of Appendix E."
tags: [sdlc, revision, evidence, review]
timestamp: 2026-09-10T17:00:00Z
---
# Adversarial review of the 2026-09-10 step review

Model: Claude Opus 5 (1M context), `claude-opus-5[1m]`.

Read: `docs/sdlc/revision/2026-09-10-step-review.md` (all 378 lines); `docs/sdlc/revision/evidence/drift.md` §2–3; `.claude/hooks/protect-approvals.sh`, `require-plan.sh`; `.sdlc/config.env`, `.sdlc/approvers.yaml`, `.sdlc/delegation.yaml`; `scripts/delegated_merge.py` (advance + merge call site), `scripts/check_artifact_chain.py` (`gh` call, superseded validation, in-progress classification, dirty-tree guard), `scripts/check_okf.py`, `scripts/gen_index.py`, `scripts/adopt.sh`, `scripts/approvers.py`; `.github/workflows/delegated-merge.yml`, `approve.yml`, `pr-review.yml`, `sdlc-gate.yml`; `.claude/agents/*.md`; `REVIEW.md`; `knowledge/decisions/merge-click-is-the-gate.md`; `work/ci-budget/{intent.md,log.md}`, `work/run-queue/plan.md`, `work/retire-active-pointer/plan.md`; `git show origin/claude/standing-grant-intent:work/standing-grant/intent.md`.

Commands run: `git log origin/main --grep='Advance'` (empty); `git log origin/main --merges -n 8` diff-name census (66 paths, 30 bookkeeping); `python3 scripts/check_okf.py` (`OKF: 189 docs, 0 warnings`); `command -v gh` (absent); `ls -d work/*/ | wc -l` (20); superseded-intent census (1 of 20).

Scope: 7 stances + 49 non-`keep` rows in Table 1 + 25 rows in Table 2 + 5 findings + Appendix C = 87 items. **62 agree, 18 dissent, 7 alternative.**

## Verdicts

### Section 0 — the seven design stances

| Item | Verdict | Counter-argument | Evidence | What would change my verdict |
|---|---|---|---|---|
| A. One source | agree | — | — | — |
| B. Plan on disk and in front of the agent | agree | — | — | — |
| C. One human act per item, at the start | **dissent** | The stance's mechanism is "risk class decides". The risk class is written by the *agent* in `intent.md`, and no hook compares it: `protect-approvals.sh` guards `status`, `mode`, `delegated-by`, `delegated-on` and the approval keys and nothing else. So the stance moves the delegation decision from a human-authored commit to an agent-authored field. That is a widening the cons column of rows 5, 8, 25 and 8.1 never names. | `.claude/hooks/protect-approvals.sh:66,93-106` (guarded keys; no `risk-class`); `git show origin/claude/standing-grant-intent:work/standing-grant/intent.md` — "`protect-approvals.sh` does not guard `risk-class` … a narrowing edit from `medium` to `low` passes every check today", and it names `knowledge/decisions/delegated-mode.md` decision 2 as claiming otherwise | A `risk-class` guard shipping *before* the standing grant, plus a `-G '^risk-class:'` human-author check in `check_artifact_chain.py`'s `check_grant`. Then stance C costs the owner one policy commit and I agree. |
| D. Bookkeeping generated by CI, never by hand and never in a diff | alternative | The measurement is right (I re-derived it: 30 of 66 changed paths across the last 8 merges are `index.md`, `log.md` or a context file — 45%, close to the draft's 43%). But "generated by CI" and "out of the tree" are two different proposals and the stance conflates them. `work/index.md` and `work/*/index.md` are *committed OKF documents* that satisfy the index-per-directory rule; removing them from the tree breaks that rule 20 times over (see row 63.1). Generating them on `main` after merge and keeping them tracked gets the whole 43% with none of that. | `for c in $(git log origin/main --merges -n 8 --format=%H); do git diff --name-only $c^1 $c; done` → 66 paths, 30 matching `index.md$|log.md$|CLAUDE.md|GEMINI.md|AGENTS.md`; `scripts/check_okf.py:178-180,192` | The stance saying "regenerated on `main` and committed", dropping the "or published as a build artifact and dropped from the tree" branch. |
| E. Models routed by a table, recorded by a script, measured before optimised | agree | — | — | — |
| F. Nothing without a consumer is documented as running | agree | — | — | — |
| G. Proven on a second repository | agree | — | — | — |

### Table 1 — the 49 non-`keep` rows

| Item | Verdict | Counter-argument | Evidence | What would change my verdict |
|---|---|---|---|---|
| 4 (interview as one numbered round) | agree | — | — | — |
| 5 (stop asking the mode; risk class decides) | **dissent** | Same defect as stance C, and here it is concrete: the step being deleted ("ask which mode the item runs under") is the *only* point where a human sees the mode choice before it binds. Replacing it with a field the agent writes and no hook guards removes the question and puts the answer in the agent's hands. The draft's con says the owner "loses a per-item veto … kept as the risk class and the policy file" — the risk class is not a human artefact. | `.claude/hooks/protect-approvals.sh:89-111`; `.claude/skills/sdlc-intent/SKILL.md:15-17` writes `risk-class` | Ship the `risk-class` guard first, or have the tap (`approve.yml`) require the class as a typed input so the human still states it. |
| 8 (skill writes risk class, mode derived) | **dissent** | See row 5. Additionally the draft's con lists the code cost ("a change to `protect-approvals.sh`, `sign.py` and the chain check") without noting that `check_grant`'s `-G '^mode: delegated$'` pickaxe exists precisely to prove a *human* authored the grant line; deriving the mode from policy deletes that proof and leaves nothing in its place. | `git show origin/claude/standing-grant-intent:work/standing-grant/intent.md` — `check_grant` at `scripts/check_artifact_chain.py:299-368` requires "the `-G '^mode: delegated$'` pickaxe finding a human author" | A replacement human-author proof named in the row (e.g. the pickaxe moves to `.sdlc/delegation.yaml`'s standing-grant block and the chain check verifies the policy commit's author). |
| 10 (ledger under 200 chars, narrative to the PR) | alternative | The cap is right; the destination is wrong. Moving the design record to a GitHub pull-request body takes it out of the committed tree, which is the kit's founding premise ("connected by committed Markdown artifacts"), and out of an adopter's clone. `work/run-queue/plan.md`'s deviations log is the counter-example: four multi-paragraph entries that are the only record of *why* four rules changed, and they are on disk. Cheaper alternative: cap the ledger `note` and send narrative to the plan's `## Deviations log`, which is already on disk, already parsed, and already the place row 54 sends deviations. | `CLAUDE.md:5-7`; `work/run-queue/plan.md` `## Deviations log` (4 entries, ~250 words each); `docs/sdlc/templates/log.md` | The row naming the plan's deviations log (or `revisions/<n>.md`) as the narrative destination instead of the pull-request body. |
| 13 (open the intent PR as a draft) | agree | — | — | — |
| 15 (one tap fragment, four inputs) | agree | — | — | — |
| 16 (merge → 10) | agree | — | — | — |
| 18 (blank slug resolved on the wrong ref) | agree | — | — | — |
| 21 (tap leaves indexes stale) | agree | — | — | — |
| 23 (drop the web-editor approval route) | alternative | The route's two failures are real, but the row's con understates the cost. On 2026-09-08 the account's Actions minutes ran out and *the tap itself died in 3 seconds* — run 34288111993, the owner's own `mode: delegated` tap. During that window the shell route is not available either, because Q14 records the owner working from a phone. Dropping the web editor leaves a phone-only owner with **no** approval route during an Actions outage, which has already happened once. Cheaper: keep the route and fix its one failure mode by making row 21's index regeneration a post-merge job (63.1's honest half), so a web-editor commit can no longer leave `main` red. | `work/ci-budget/intent.md` "the `approve` dispatch run 34288111993 at 22:53:34, the owner's `mode: delegated` tap … dead after 3 seconds"; Appendix A Q14 (phone) | Evidence that the owner has a shell on the phone, or that the tap survives a quota block. |
| 25 (standing grant for `risk-class: low`) | agree | — | (my objection is to 8.1's mechanism, not to a human-committed standing grant) | — |
| 26 (drop the manual `log_ledger.py` confirmation) | agree | — | — | — |
| 35 (`sign.py --commit`) | agree | — | — | — |
| 38 (file list becomes the guard's input) | agree | — | — | — |
| 42 (five-field tickable plan steps) | agree | — | — | — |
| 45 (edit-time file-list guard beside `require-plan.sh`) | **dissent** | See row 45.1: as scoped it fires repo-wide, not under `PLAN_REQUIRED_PATHS`, and it collides with `max-deviations: 5`. | `.sdlc/config.env:4` `PLAN_REQUIRED_PATHS="scripts"`; `.sdlc/delegation.yaml` `max-deviations: 5` | See 45.1. |
| 48 (add five paths to `PROTECTED_PATHS`) | alternative | Two of the five are already covered and the row's framing overstates the gap. `REVIEW.md` and `.claude-plugin` are in `.sdlc/delegation.yaml`'s `locked-paths`, so a delegated merge already refuses a diff that touches them; and `pr-review.yml` pins `REVIEW.md` from the base ref into `/tmp/REVIEW.base.md` before the reviewer reads anything, so a head-branch rewrite of the review policy is already inert. Adding all five is not wrong, but the honest list of *uncovered* control-plane files is `.claude/skills/`, `.claude/agents/` and `CODEOWNERS`. | `.sdlc/delegation.yaml` `locked-paths: [… REVIEW.md, .claude-plugin]`; `.github/workflows/pr-review.yml:64-69` "Pin review policy from base branch"; `.sdlc/config.env:9` | Nothing — I agree with the direction; I want the row to stop claiming a gap that two existing controls already close, so the owner prices the change correctly. |
| 54 (deviation written when the guard fires) | agree | — | — | — |
| 58 (review skill writes writer/reviewer models) | agree | — | — | — |
| 59 (start the measurement the one-writer decision waits on) | agree | — | — | — |
| 60 (bound `Bash` in each agent file) | alternative | The finding is correct — all four agents carry bare `Bash` — but the fix as written is not expressible. A subagent's `tools:` front matter takes tool *names*, not per-command allowlists; there is no syntax for "Bash bounded to `git diff`". The `.gemini` mirrors do it in prose, which is advice, not a bound. Cheaper and actually true: **remove** `Bash` from `explorer.md` and `security-reviewer.md` (neither runs a command — their prompts are pure read-and-cite), keep it on `plan-reviewer.md` (`git diff --name-only`) and `verifier.md` (`verify.sh`), and fix the three documents to say two of four run commands. | `.claude/agents/explorer.md:4`, `security-reviewer.md:4`, `plan-reviewer.md:4-6`, `verifier.md:4-6` (all `tools: Read, Grep, Glob, Bash`); `evidence/drift.md` §2 severity-high entries 1–2 | A worked example of a `tools:` line that bounds `Bash` to an allowlist in this harness. Then I agree with the row verbatim. |
| 62 (park item-local stops instead of stopping the queue) | agree | — | — | — |
| 63 (indexes stop being committed) | **dissent** | See row 63.1. The generated half (context files stay) is right; the "stop being committed" half breaks the OKF rule the draft's own row 70 says to keep. | `scripts/check_okf.py:178-180,192` | See 63.1. |
| 64 (front-matter check reads untracked files) | agree | — | — | — |
| 65 (guard the `gh` call, print sha on FAIL) | agree | — | — | — |
| 71 (spend freed context lines on a style fragment) | agree | — | — | — |
| 72 (Stop hook names unplanned paths) | agree | — | — | — |
| 73 (pin the verifier's model) | agree | — | — | — |
| 78 (bound the reviewer subagents' tools) | agree | — | (same substance as row 60; my alternative there applies) | — |
| 84 (one lesson destination) | agree | — | `REVIEW.md:13` does say `CLAUDE.md` "Lessons learned"; verified | — |
| 92 (drop the CI failure triage) | **dissent** | The replacement does not exist. `pr-review.yml`'s `@claude` route reviews the **diff** against `REVIEW.md`; its prompt says "Do not run any command", it carries `--disallowedTools "Bash,Edit,Write,…"` and `--allowedTools` only the two comment MCP tools, so it cannot open a failing job log. It is also gated on `author_association` in `["OWNER","MEMBER","COLLABORATOR"]`, i.e. it needs a **human comment** — dropping the triage therefore adds a human act, against stance C. Separately, the owner already answered this question: "the failure triage, removed or kept behind a `triage` label? … **A: label (owner, 2026-09-09)**", in an intent that is now `status: approved`, `approved-by: luissiviero`, `approved-on: 2026-09-10`. Reversing it is a change to an approved artifact, which under `.sdlc/delegation.yaml`'s `revisions: consensus` needs a `revisions/<n>.md`, not a table row. | `.github/workflows/pr-review.yml:78-98` (prompt + `claude_args`), `:43-45` (author_association gate); `work/ci-budget/intent.md` open-questions block, "A: label (owner, 2026-09-09)"; `work/ci-budget/intent.md` front matter `status: approved` | The owner saying in this revision that they have changed their 2026-09-09 answer — then it is a decision, not a defect, and the row should say so and cite the reversal. |
| 93 (drop the duplicate `hook-cases` PR job) | agree | — | — | — |
| 94 (concurrency on `pr-review`) | **dissent** | The row omits the qualifier the owner's approved answer requires. `scripts/delegated_merge.py` treats a **cancelled** run on the head sha as refusing (`OK_CHECK_CONCLUSIONS = ("success", "skipped", "neutral")`); the ci-budget intent records the rule and its safety condition: "with cancel-in-progress bound to `synchronize`, a `cancelled` run never lands on the head sha". Row 94 says only "a `concurrency:` group that cancels the superseded run" and 94.1 says "keyed on the pull request, `cancel-in-progress: true`" — unbound, so an `edited`, `labeled` or `ready_for_review` event cancels a run **on the same sha the merge script will judge**, and that head becomes permanently unmergeable. | `scripts/delegated_merge.py:78`; `work/ci-budget/intent.md`, second-session Q&A on the skipped-run rule | The row adding "bound to `synchronize`" (or an equivalent same-sha exclusion). |
| 96 (create the branch-protection rule) | **dissent** | Protection and the queue collide, and the row's con only considers the merge API. `advance()` does not merge — it **pushes a commit straight to `main`** (`git push origin HEAD:<branch>`). A required status check on `main` rejects a direct push carrying no run of that check, so the day `sdlc-gate` becomes required, row 100.1's newly-working advance push starts failing again for a different reason; and 63.1's "CI … commits them as the bot" fails the same way. | `scripts/delegated_merge.py:993-1000` (the direct push); `.github/workflows/delegated-merge.yml` `permissions: contents: write` | The row naming the bypass (a ruleset bypass actor for `github-actions[bot]`, or routing the advance through a pull request) — then I agree. |
| 97 (workflow wake list equals the policy) | agree | — | — | — |
| 100 (fix the advance push) | agree | Verified: `git log origin/main --grep='Advance'` is empty, and the workflow checks out `ref: default_branch` *before* the merge, so the local `main` is one commit behind by the time `advance()` pushes. | `git log origin/main --grep='Advance'` → no output; `.github/workflows/delegated-merge.yml` "The ONLY checkout, and it takes the default branch"; `scripts/delegated_merge.py:993-1000` | — |
| 104 (retire on merge / tap gains `artifact: retire`) | **dissent** | See row 104.1. The diagnosis is right (verified: 1 of 20 intents is `superseded`); the remedy is under-specified in three ways that each break something. | `for d in work/*/; do grep -m1 '^status:' $d/intent.md; done` → 1 superseded of 20; `scripts/check_artifact_chain.py:733-758` | See 104.1. |
| 105 (retirement ledger line written by the same tool) | agree | — | — | — |
| 106 (merge moves the pointer, human never touches it) | agree | — | — | — |
| 107 (drop the post-retirement regeneration) | agree | — | (correct *if* 63 is accepted; my objection is to 63) | — |
| 124 (drop the `triage` label) | **dissent** | Follows row 92, so it inherits row 92's defect, and it reverses a second explicit owner answer: "the `triage` label does not exist yet … Proposed: the owner creates it before the control-plane pull request goes ready. **A: yes (owner, 2026-09-09**, with the labels URL)". | `work/ci-budget/intent.md`, last open question | The same as row 92. |
| 125 (require `sdlc-gate` only) | agree | — | — | — |
| 133 (standing grant in the policy file) | agree | — | (the human-committed policy line is fine; 8.1's agent-signed intent is not) | — |
| 135 (replace the loop-automation prose) | agree | — | — | — |
| 136 (retarget the eval corpus) | agree | — | — | — |
| 146 (diagnose a breach once) | agree | — | — | — |
| 161 (drop scheduled scans from the play table) | agree | — | — | — |
| 162 (drop on-call from the play table) | agree | — | — | — |
| 163 (mark each metrics row produced / no source) | agree | — | — | — |

### Table 2 — the 25 new steps

| Item | Verdict | Counter-argument | Evidence | What would change my verdict |
|---|---|---|---|---|
| 4.1 | agree | — | — | — |
| 8.1 (standing grant; agent signs the intent's `mode`) | **dissent** | The step contradicts the branch it cites and the policy it edits. (a) `.sdlc/delegation.yaml`'s `signable: [spec.md, plan.md, incident.md]` excludes `intent.md` **by design**, and says why: "the grant lives there and the agent must never be able to widen its own permission by signing the file that grants it". (b) The `standing-grant` intent explicitly does *not* do what 8.1 says: it has the intent carry "`status: delegated`, `approved-by: <agent handle>` and **a third mode word written only by the signing script (never `mode: delegated`, which the hook refuses on an agent write and whose pickaxe must keep finding a human)**". 8.1's phrasing, "the agent signs the intent's `mode` under that grant", is precisely the thing that design avoids. (c) The gating input, `risk-class`, is agent-written and unguarded, so as drafted an agent can type `risk-class: low` and delegate its own item end to end with no human commit anywhere. The cons column says only that the policy file's `risk-classes` line "becomes the single most consequential line in the repo"; the consequential line is actually the one in `intent.md`. | `.sdlc/delegation.yaml` `signable:` block and its comment; `git show origin/claude/standing-grant-intent:work/standing-grant/intent.md` (both the "third mode word" outcome and the "does not guard `risk-class`" finding); `.claude/hooks/protect-approvals.sh:66,100,126` | Rewrite 8.1 to (i) keep `intent.md` unsignable, (ii) use the branch's third word rather than `mode:`, (iii) list a `risk-class` guard as a prerequisite step, not a footnote. Then I agree. |
| 13.1 | agree | — | — | — |
| 35.1 | agree | — | — | — |
| 42.1 | agree | — | — | — |
| 42.2 | agree | — | — | — |
| 45.1 (edit-time file-list guard) | **dissent** | Two problems the cons column does not price. (a) **Scope**: unlike `require-plan.sh`, which only fires `under_any "$R" "$PLAN_REQUIRED_PATHS"` (`"scripts"`), 45.1 fires on *any* path. In this repo a plan-covered session legitimately writes `work/<slug>/log.md` at every gate, `revisions/<n>.md` on a review round, new `knowledge/lessons/*.md` under rule 7, and the three regenerated context files. (b) **It collides with `max-deviations: 5`.** `work/run-queue` is the worked example: its deviations log spent deviation 1 on three files (a test, a lesson, a rules fragment), deviation 2 on two more found by the conformance pass, the review round on two more, and deviation 4 on six documentation files and two lessons — i.e. **4 of the 5 permitted deviations went on files no plan could have foreseen**, almost all bookkeeping. Turn each of those into an exit-2 block that must be answered with a deviation line and a delegated run hits the cap and stops on paperwork. | `.claude/hooks/require-plan.sh:16`; `.sdlc/config.env:4`; `.sdlc/delegation.yaml` `max-deviations: 5`; `work/run-queue/plan.md` `## Deviations log` (4 entries) and the commit `a8b7001` "[run-queue] Documentation audit before the session ends (deviation 4 of 5)" | Either scope the guard to `PLAN_REQUIRED_PATHS` (where the file-list contract actually matters), or exempt `work/<slug>/`, `knowledge/lessons/` and `GENERATED_PATHS` by prefix *and* stop counting those additions against `max-deviations`. Either fix and I agree. |
| 58.1 | agree | — | — | — |
| 58.2 | agree | — | — | — |
| 60.1 | agree | — | (the `Input:` half is right and cheap; the `Bash` half is the row-60 alternative) | — |
| 62.1 (`parked`) | alternative | A `parked` outcome recorded in the ledger forces `parked` into the *artifact status* vocabulary, because `knowledge/lessons/ledger-slot-holds-status-only.md` is a standing rule that the from/to slot holds status values only, and `STATUSES` is a fixed five-tuple that `protect-approvals.sh`, `sign.py` and `check_artifact_chain.py` all switch on. That is a much larger blast radius than "new vocabulary for the chain check and the indexes". Cheaper: park with no new word — leave the pull request a draft, write the reason as a `deviation:`-style note on an existing status-to-same-status line (the shape `advance()` already uses: `in-review -> in-review`), and let `next_item.py` skip an item whose newest ledger note starts `parked:`. | `scripts/check_artifact_chain.py:60` `STATUSES = ("draft", "in-review", "approved", "delegated", "superseded")`; `knowledge/lessons/ledger-slot-holds-status-only.md` (pointer in `CLAUDE.md`); `scripts/delegated_merge.py:958-962` (the existing same-status ledger line) | A count of every switch on `STATUSES` showing the addition is cheap. |
| 63.1 (indexes out of the tree) | **dissent** | It breaks the OKF index-per-directory rule 20 times. `check_okf.py`'s `default_paths` is `KNOWLEDGE_PATHS + ["work"]`, and `scan()` warns `missing index.md` for any directory that holds `.md` files and no `index.md`. Each of the 20 `work/<slug>/` directories holds `intent.md`, `spec.md`, `plan.md`, `log.md`; deleting `work/<slug>/index.md` turns today's `OKF: 189 docs, 0 warnings` into 20 warnings, and `CLAUDE.md`'s verification section requires `0 warnings`. The draft's own row 70 says to keep the check "at what it is (front matter, links, **an index per directory**)" — 63.1 and 70 cannot both be accepted. It also breaks the adopter path: `adopt.sh` writes `work/index.md` and `work/_example/index.md` into the install plan's file list. | `scripts/check_okf.py:192` (`+ ["work"]`), `:178-180` (the warning); `python3 scripts/check_okf.py` → `OKF: 189 docs, 0 warnings`; `ls -d work/*/ | wc -l` → 20; `scripts/adopt.sh:514`; draft row 70 | Take the first branch only — CI regenerates and commits the indexes on `main` after each merge, they stay tracked, `index-drift.sh` runs on `main` only. That gets the whole 43% saving with no OKF change. Say that and I agree. |
| 71.1 | agree | — | — | — |
| 72.1 | agree | — | — | — |
| 94.1 (concurrency, `cancel-in-progress: true`) | **dissent** | See row 94: without binding cancellation to `synchronize`, a cancelled run lands on the head sha the merge script judges, and `OK_CHECK_CONCLUSIONS` does not include `cancelled`, so the pull request never merges. The owner's approved answer contains the binding; the row drops it. | `scripts/delegated_merge.py:78`; `work/ci-budget/intent.md` second-session skipped-run Q&A | Add "bound to `synchronize`". |
| 96.1 (branch protection) | **dissent** | Same as row 96, and 96.1 makes it worse by adding "no force pushes" and a review requirement without saying what happens to the two direct-to-`main` bot writes the review itself proposes (100.1's advance commit and 63.1's index commit). A required status check on `main` blocks a direct push whose commit has no such check. Sequencing matters: Appendix C puts protection at step 2 and the advance at step 1, so the advance is fixed and then broken again one sitting later. | `scripts/delegated_merge.py:993-1000`; draft rows 63.1 and 100.1; Appendix C steps 1–2 | The row naming a bypass actor for `github-actions[bot]`, or dropping the direct-push design in favour of a pull-request-based advance. |
| 100.1 (fetch and fast-forward before the advance) | agree | — | Verified the mechanism and the symptom independently; the bare-remote regression test is the right shape | — |
| 104.1 (delegated items retire on merge) | **dissent** | Three under-statements, in order of how much is at stake. (a) **Which merge?** `advance()` runs after *any* successful delegated merge of the active item, on the comment "the merge is the moment the item finishes". An item routinely has more than one pull request (intent, spec, plan, code). Retiring on the first one would set `superseded` on `spec.md` and `plan.md` before the code lands, and — via row 108 — close the plan gate mid-item. The row needs a "last pull request" test and has none. (b) **The allowlist.** The advance commit is bounded to exactly three paths (`.sdlc/active` plus two `log.md` files) and abandons the commit if anything else is staged; that guard exists because of a security pass. Retirement widens it to arbitrary `work/<slug>/*.md`, and those writes go **straight to `main` by direct push**, with no pull request, no `sdlc-gate` run, and no `control-plane-approved` label — `check_control_plane.sh` only runs on pull requests. (c) **The code cost.** The row says it "amends `human-only-approvals.md`". It also requires editing `check_artifact_chain.py` (which validates `superseded`'s `approved-by` through `av.is_valid`, and `claude` is in `never-approve`) and the ledger match that demands a **valid human approver** as the actor of the `-> superseded` entry. `scripts/check_artifact_chain.py` is itself in `.sdlc/delegation.yaml`'s `locked-paths`, so the change can never ship in a delegated pull request. None of that is in the cons column, which lists only "`superseded` is no longer a word only a human writes". | `scripts/delegated_merge.py:872-878` and the comment above it; `:887-990` (`ADVANCE_IDENTITY`, the `written` allowlist, "refusing to commit paths outside the advance allowlist"); `:993-1000` (direct push); `scripts/check_artifact_chain.py:733-758`; `.sdlc/approvers.yaml` `never-approve: ["claude[bot]", "github-actions[bot]", "claude"]`; `.sdlc/delegation.yaml` `locked-paths` | A "last pull request" predicate (e.g. the plan's `## Order of work` fully ticked, per 42.1), retirement routed through a pull request rather than a direct push, and the cons column naming the chain-check and approvers-policy edits. With those three I agree — the defect is real and 19 open items is not a tenable end state. |
| 135.1 (definition of done) | alternative | The definition is right; the freeze clause is self-contradictory. "Until that is met, no new roadmap item starts" would block Appendix C steps 1–8, which are themselves new work items — including the four live defects the same document calls the precondition for anything being trustworthy. | Draft row 135.1 vs Appendix C step 1 ("Nothing else is trustworthy until the queue actually queues") | Scope the freeze to roadmap items that are *not* named in Appendix C, or drop the clause and keep the criterion. |
| 135.2 (revision charter) | agree | — | — | — |
| 146.1 | agree | — | — | — |
| 161.1 | agree | — | — | — |
| 163.1 | agree | — | — | — |
| A.1 (undocumented flags and variables) | agree | — | — | — |
| A.2 (one control-plane PR for every locked-path change) | **dissent** | This is the largest un-reviewed blast radius the review proposes, and its con ("a large diff to read") does not name the mechanism. Row 87 records, as an accepted residual, that the `control-plane-approved` label **persists across pushes**. One label on one branch that carries rows 8, 21, 35, 45.1, 48, 65, 97, 98, 100.1 and 104.1 therefore pre-approves every subsequent push to that branch — every hook, every workflow, `.sdlc/`, and the two scripts that judge merges and approvals — for as long as the branch is open. It also bundles the one row I most want reviewed on its own (104.1, which changes who may write `superseded`) with four mechanical one-line fixes, so a reviewer who wants the fixes has to take the semantics too. Contradicts Appendix C step 1, which correctly scopes the first control-plane pull request to four defects. | Draft row 87 ("the label persists across pushes … recorded and accepted"); `scripts/check_control_plane.sh`; Appendix C step 1 vs A.2 | Split it as Appendix C already implies: one control-plane pull request for the four mechanical defects (100.1, 65, 97, 98), a second for the guards (45.1, 48), a third for 104.1 alone. Three labels is still fewer than one per item. |

### The five findings, and Appendix C

| Item | Verdict | Counter-argument | Evidence | What would change my verdict |
|---|---|---|---|---|
| Finding 1 (the advance has never landed) | agree | Reproduced independently: `git log origin/main --grep='Advance'` returns nothing, and the workflow's only checkout is `ref: default_branch` taken *before* the merge, so the local `main` is one commit stale when `advance()` pushes. The Actions run tallies I could not re-derive (no `gh` in this container), but the git evidence alone carries the finding. | `git log origin/main --grep='Advance'` (empty); `.github/workflows/delegated-merge.yml` checkout comment; `scripts/delegated_merge.py:993-1000` | — |
| Finding 2 (retiring a delegated item is impossible; 19 of 20 open) | agree | Reproduced: 1 of 20 `work/*/intent.md` is `superseded`, and `check_artifact_chain.py` runs `av.is_valid` on a `superseded` artifact's `approved-by`, which for a delegated artifact is `claude` — listed in `never-approve`. | superseded census (1 of 20); `scripts/check_artifact_chain.py:733-739`; `.sdlc/approvers.yaml` `never-approve` | — |
| Finding 3 (reviewer subagents documented read-only, all four carry `Bash`) | agree | Verified all four files. One caveat that belongs in the finding: `REVIEW.md` and `.claude-plugin` are already in `.sdlc/delegation.yaml`'s `locked-paths`, and `pr-review.yml` pins `REVIEW.md` from the base ref, so the "the agent can rewrite … its reviewers" half is true for `.claude/agents/` and `CODEOWNERS` and overstated for `REVIEW.md`. | `.claude/agents/*.md:4`; `.sdlc/delegation.yaml` `locked-paths`; `.github/workflows/pr-review.yml:64-69` | — |
| Finding 4 (`VERIFY: FAIL` with a token and no `gh`) | agree | Verified the exact mechanism: `check_artifact_chain.py:201` calls `subprocess.run(["gh", "api", …])` with no `FileNotFoundError` guard, while the token check one line earlier at `:196` returns cleanly. `command -v gh` finds nothing in this container. | `scripts/check_artifact_chain.py:196` vs `:201`; `command -v gh` → not found | — |
| Finding 5 (the CI-is-informational premise ended when the repo went public) | agree | Verified. One nuance worth adding rather than a dissent: the decision is not silent — it already carries its own trigger, "Revisit if the repo goes public or the account moves to Pro/Team: protection rules then become a one-time settings task, **and this record is superseded**". So the finding is better stated as "the decision superseded itself on 2026-09-09 and nobody wrote the banner", which strengthens rows 96 and 96.1. | `knowledge/decisions/merge-click-is-the-gate.md`, Consequences, last bullet; `work/ci-budget/log.md` 2026-09-09T15:20 entry ("`main` carries no branch protection rule at all (branches API, `protected: false`)") | — |
| Appendix C, order of work | **dissent** | Two ordering defects. (a) Step 1 fixes the advance push (100.1) and step 2 turns on branch protection (96.1) — but a required status check on `main` blocks the very direct push step 1 just repaired, so step 1's work is undone by step 2 unless the bypass is designed first. Protection must be specified *with* the advance, not after it. (b) Step 1 bundles 104.1 with three mechanical fixes; 104.1 is the only proposal in the document that changes who may write a chain-artifact status, and it is the one that most needs to be readable on its own diff. Also: step 3 carries row 92, which I dissent on, and step 7 ("one act per item", last, "because it widens what runs without the owner") correctly identifies 8.1 as the widening — but the widening it names is the standing grant, not the ungated `risk-class` field that actually opens the door. | `scripts/delegated_merge.py:993-1000`; draft rows 96.1, 100.1, 104.1; Appendix C steps 1, 2, 3, 7 | Reorder: (1) 65, 97, 98, 100.1 with the protection bypass designed alongside; (2) 96.1 + 125; (3) 104.1 alone; then the rest as drafted. |

## Missed

Rows the draft marked `keep` (or did not raise) that are broken, with evidence:

- **`.sdlc/delegation.yaml`'s `max-deviations: 5` is mis-sized against practice, and row 55 marks it `keep`.** `work/run-queue` spent 4 of its 5 permitted deviations, and by its own log almost all of them on files no plan could foresee: a lesson file and a rules fragment under rule 7 (deviation 1), two files found by the conformance pass (deviation 2), two more from the review round, and six documentation files plus two lessons (deviation 4). The cap is therefore already a bookkeeping cap, not a scope cap — and row 45.1 would make every one of those a hard block. Evidence: `work/run-queue/plan.md` `## Deviations log`; commit `a8b7001` subject "(deviation 4 of 5)"; `.sdlc/delegation.yaml` `max-deviations: 5`. This should be a `change` row: exclude `work/<slug>/`, `knowledge/lessons/` and `GENERATED_PATHS` additions from the count.
- **`protect-approvals.sh` does not guard `risk-class`, while `knowledge/decisions/delegated-mode.md` decision 2 says it guards all four grant keys.** This is a live contradiction between a decision record and the hook that implements it, of exactly the kind Appendix B collects — and it is the load-bearing gap under stance C, rows 5, 8, 25, 133 and 8.1. Appendix B does not list it and no row is marked `fix`. Evidence: `.claude/hooks/protect-approvals.sh:89-111` (compares `status`, `mode`, `delegated-by`, `delegated-on`, approval keys — never `risk-class`); `git show origin/claude/standing-grant-intent:work/standing-grant/intent.md` states the same finding explicitly.
- **`advance()` fires on the first merged pull request of an item, not the last, and row 98 / row 100 / row 103 all mark that `keep`.** The code's own comment states the assumption — "the merge is the moment the item finishes" — which is true only for one-pull-request items. `work/run-queue` and `work/retire-active-pointer` each ran several. Today the consequence is a premature pointer move; under row 104.1 it becomes a premature retirement. Evidence: `scripts/delegated_merge.py:872-878`.
- **Row 87 is marked `keep` on the grounds that the label residual is "recorded and accepted", but A.2 changes its magnitude.** A persistent label on a small item-scoped diff is a small residual; the same label on a branch touching every hook, every workflow, `.sdlc/`, `check_artifact_chain.py` and `delegated_merge.py` is not the same object. If A.2 is accepted, row 87 needs to become `change`: re-require the label after any push that adds a new `PROTECTED_PATHS` file to the diff.

## Ranked dissents

1. **8.1 (with stance C, rows 5 and 8)** — the standing grant keys on `risk-class`, a field the agent writes and no hook guards, and 8.1 has the agent sign `intent.md`'s `mode`, which `.sdlc/delegation.yaml`'s `signable` list and the branch it cites both forbid by design; as drafted an agent could delegate its own item with no human commit anywhere.
2. **104.1** — retirement on merge fires on the item's *first* merged pull request, widens the advance commit's three-path allowlist to arbitrary chain artifacts, and writes them straight to `main` with no pull request, no gate and no label; the cons column names only the loss of "human-only `superseded`".
3. **63.1** — moving `work/*/index.md` out of the tree turns `OKF: 189 docs, 0 warnings` into 20 warnings and contradicts the draft's own row 70; the first half of the proposal (CI regenerates and commits on `main`) captures the whole 43% saving without it.
4. **92 / 124** — the `@claude` route cannot read a failing job log (`--disallowedTools "Bash"`, "Do not run any command") and needs a human comment to fire, so dropping the CI triage loses the capability and adds a human act; it also reverses two answers the owner gave in an intent that is now approved.
5. **45.1** — a repo-wide edit-time file-list guard collides with `max-deviations: 5`, and `work/run-queue` spent 4 of its 5 deviations on exactly the unforeseeable bookkeeping files the guard would block.
6. **94.1** — dropping the `synchronize` binding from the owner's approved answer leaves cancelled runs on the judged head sha, which `OK_CHECK_CONCLUSIONS` refuses: a permanently unmergeable pull request.
7. **96.1 (and Appendix C's order)** — a required status check on `main` blocks the bot's direct advance push, so protection as drafted breaks the queue that step 1 just repaired.
8. **A.2** — one persistent `control-plane-approved` label over a branch touching every locked path in the repository is the largest un-reviewed surface the review proposes, and it contradicts Appendix C step 1.
