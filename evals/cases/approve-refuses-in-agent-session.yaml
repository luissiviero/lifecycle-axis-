name: approve-refuses-in-agent-session
kind: hook
source: work/sdlc-kit-phase-1/plan.md (deviation: option C approval helper)
check: |
  cd "$(git rev-parse --show-toplevel)" && ! CLAUDECODE=1 python3 scripts/approve.py _example plan.md --as luissiviero --dry-run 2>/dev/null
