---
type: sdlc/log
id: session-chaining-log
title: Gate ledger for session-chaining
description: Chronological record of stage transitions and approvals for this work item.
timestamp: 2026-09-13T16:30:00Z
---
# Log: session-chaining

Format: `- <RFC3339> | <artifact> | <from> -> <to> | <actor> | <sha> | <note>`
(append-only; `<note>` is optional; parsed by `scripts/log_ledger.py`).

- 2026-09-13T16:30:00Z | intent.md | (none) -> in-review | claude | 6cd63b1 | drafted from the owner's two questions during the ci-budget execution session, quoted verbatim, and from the three defects the mock walk of 2026-09-11 found, each re-verified against main at 6cd63b1 before being written down. Defect 2 is worse than the mock walk recorded: sdlc-review does not merely name a CLAUDE.md section that no longer exists, it tells the agent to edit CLAUDE.md at all, which is a generated file whose hand-edit context-drift.sh fails. Defect 3 confirmed by count: sdlc-intent carries zero mentions of log.md or gen_index.py, so an item created by following it literally has no ledger and drifts the index. Three open questions carried for the owner, the first of which decides whether this stays a documentation item at risk-class low or becomes a mechanism and is re-examined. This ledger and the index regeneration are themselves the behaviour defect 3 is about, done here by hand because the skill does not yet ask for them. Writer opus
- 2026-09-13T20:24:28Z | intent.md | in-review -> approved | luissiviero | 2580e6e
- 2026-09-13T20:45:00Z | spec.md | (none) -> in-review | claude | 1abded5 | nine requirements from the intent's outcomes plus the owner's three answers, four areas of concern, three decisions. A read-only scout on sonnet mapped the repository's existing agent-from-CI precedents before any shape was chosen, and what it found changed the answer: run-queue.md lines 26 to 28 had already closed the route where a session wakes a session, every agent CI in this repository is read-only and advisory, adopt.sh copies five workflows and not delegated-merge.yml, and production-gate.sh blocks an agent dispatching a workflow. The owner chose the session-side shape over a CI spawner on that evidence. The design splits the mechanism: the kit owns the contract, a seed prompt committed to git and refreshed by the finishing session, and the runtime owns the trigger, so no kit code calls any vendor API and a runtime without scheduling skips that step and says so. The ordering in R2 is the safety argument, since the durable state is committed before anything is scheduled, which is also why this sits beside run-queue.md rather than against it: that decision closed a session being RELIED ON, and nothing here requires a live session for correctness. C1 records that risk-class low still holds on its own terms because the shape adds no script, no workflow, no secret and no authority, with R9 as the machine check that stops the item if Build finds otherwise. C2 raises the one genuinely new exposure, a session that can schedule successors, and bounds it three ways. pointer-contradiction folded in as R7 on the owner's answer, since HANDOFF line 37 instructs writing .sdlc/active, which PROTECTED_PATHS blocks and sdlc-run line 71 forbids, the same defect class as R5 in a file R1 rewrites. Writer opus, scout sonnet
- 2026-09-13T21:05:00Z | spec.md | in-review -> in-review | claude | 1e7c75f | two reviews on sonnet before the tap, both applied, every finding verified against the code first. Conformance: 1 blocking, 4 important, 1 nit. The blocking one was mine repeating my own lesson, which this repository merged three hours earlier: R8 offered wc -l on the adopter render as proof the new line landed, and that render already prints 120 on an unchanged tree, so the clause could never fail. It now asserts grep -c HANDOFF on the adopter's rendered CLAUDE.md, which is 0 today, with wc -l kept and labelled a ceiling rather than proof. Also fixed: the Design section carried none of the template's four subheadings while all sixteen other specs carry them; R7 grepped one exact phrase and missed the twin wrong claim at HANDOFF line 194, so it now greps the shape and catches both, 2 today; R4 cited a standing invariant that was already true and could never discriminate; and the run-queue citation was off by one, 25 to 27 not 26 to 28. Security: 2 major, 1 minor, and the first is an escalation this spec itself introduced. docs/sdlc/handoff is on neither the merge script's ALWAYS_LOCKED floor nor the policy locked-paths, while delegation is enabled and merge.enabled is true, so once R3 makes that file's prose the next session's instructions a delegated pull request touching only the handoff could widen the seed prompt and merge with no human click. Recorded as C5 with the one-line fix the owner must take before Build, since R9 forbids this item touching .sdlc and the policy file is human-only. R10 added as defence in depth: the prompt is context, never authority, and the successor re-reads the item's approved artifacts. C2's third bound was overstated and is corrected to say plainly that committing the prompt is a convention rather than a guarantee, because R4 puts the call in the runtime by design and nothing observes the payload. R2 gained an at-most-once cap, citing the account that already exhausted its minutes once. Writer opus, reviewers sonnet
- 2026-09-13T22:08:59Z | spec.md | in-review -> approved | luissiviero | f692c0e
- 2026-09-13T22:40:00Z | plan.md | (none) -> in-review | claude | d035c6e | C5 closed first: the owner added docs/sdlc/handoff to locked-paths at d035c6e, and check_locked_paths was shown to refuse HANDOFF.md before this plan was written. One pull request, fourteen paths, instructions and documentation only, with R9 as a machine check in the order of work rather than a promise. Two consequences of the approved spec are stated up front for the owner to decide at the tap: the per-cycle handoff refresh is a second, handoff-only pull request, since R2's step (a) is only answerable after the item's own pull request has merged, and with the handoff now locked that pull request is the owner's click every cycle, so sdlc-run's stop rule gains one named exception on the grounds the spec's Design already gives, that the successor's correctness rests on .sdlc/active and its payload and never on the refresh landing. The R8 payment site was chosen by reading adopt.sh rather than by counting: the obvious three-line intro of 00-chain.md is replaced byte-for-byte by adopt.sh:493-499 and pinned by test_adopt.py:485-488, so a re-flow there ships the wrong paragraph to adopters silently; the line is paid from 30-conventions.md instead, and the proof is the adopter's render, not the repository's. Every proof line carries the value it prints on main today, so a reviewer can see each one is red before the change. Step 8 runs the new act on this item's own merge as its first live exercise, expecting R4's honest fallback since a supervised item's pointer does not move on merge. Writer fable
- 2026-09-13T23:20:00Z | plan.md | in-review -> in-review | claude | 4ce9164 | two reviews on sonnet, six findings, all verified against the tree before being applied. Security, 1 major: the stop-rule exception lets the successor start from a pushed, unmerged prompt, so it acts on handoff content before the owner's click; that is R2's own order, push then schedule, and the C5 lock governs the merge and not the scheduling, but the plan had left it implied, and now says it in plain words as the trade the owner is taking at the tap, bounded by R10 and the hooks. Two minor: the merge click was framed as C5's doing alone when ALWAYS_LOCKED already covers ten of the fourteen paths and the item is supervised besides; and step 8 claimed .sdlc/active names session-chaining, when it reads ci-budget and a supervised merge moves it nowhere, advance() running only inside the delegated merge. Conformance, 2 important: no spec row authorises or pins the stop-rule sentence, so it is now grepped by the eval case and named in step 4 and the Proof, 0 today; and step 8 said "step 7 runs" for an item whose mode fails sdlc-run's precondition, so it now says the session applies the act by hand. One nit: the re-flow bullet's widths are 104 to 117, not 102 to 118. Writer fable, reviewers sonnet
