---
type: sdlc/revision
id: chain-check-without-gh-revision-2
title: The plan's file list names the same locked script; the route of revisions/1.md is confirmed at the plan gate
description: "Detour record at the plan gate: the plan's file list ends DETOUR: needed (1) on scripts/check_artifact_chain.py, the same one path revisions/1.md routed around; the route is unchanged, build to a ready pull request and park with click needed, now with the plan's order of work and the park mechanics written down."
artifact: plan.md
# kind: revision | detour  (detour: the trigger is a DETOUR needed line from scripts/check_detour.py and the proposal is a route that stays low; scripts/sign.py and the chain check read both kinds the same way)
kind: detour
trigger: "python3 scripts/check_detour.py --slug chain-check-without-gh --plan work/chain-check-without-gh/plan.md -> 'scripts/check_artifact_chain.py: locked by locked-paths scripts/check_artifact_chain.py' / 'DETOUR: needed (1)', exit 3, on 4ba5de7"
timestamp: 2026-09-18T17:25:00Z
---
# Revision 2: the plan's file list names the same locked script; the route of revisions/1.md is confirmed at the plan gate

## Proposal
Nothing in `plan.md` changes: it was written to the route `revisions/1.md` adopted at the spec gate, and its
own detour check names the same single path for the same reason. What this record adds is the plan-level
shape of the route, so the reviewers judge it where it is now concrete: the file list (two code paths, one
locked, one new and unlocked), the order of work (red module, fix, the two production shapes with the token
set, the whole loop, review and ready, then the park), and the park mechanics (a ledger-only pull request off
`origin/main`, merged by the delegated merge whose advance clears the pointer on the empty queue, then
`origin/main` merged back into the item branch so #111 is clean for the click). The trigger is committed in
this record's front matter and stated in the plan's preamble.

Why this is the smallest change that clears the trigger: as at the spec gate, the trigger names one path and
that path is the defect; the plan lists nothing else under `scripts/` but the new module, which is unlocked.

## Route (detour only)
- Outcome reached: every spec requirement, R1 to R7, through the plan's steps 1 to 4; the merge by click
  through step 5. None is left out.
- Paths and their lists, as `scripts/check_detour.py --plan` prints them over the plan's `## Files that
  change`:
  - `scripts/check_artifact_chain.py`: `locked by locked-paths 'scripts/check_artifact_chain.py'`. Edited
    under this signed plan (`require-plan.sh` opens on `status: delegated`; the file is not under
    `PROTECTED_PATHS`); refused by the delegated merge; merged by the owner's click on #111.
  - `scripts/test_chain_no_gh.py` (new): no list; writable under `kind: fix` because it is new.
  - `work/chain-check-without-gh/plan.md`, `log.md`, `revisions/2.md`, `revisions/index.md`, `index.md`,
    `work/index.md`: the item's own artifacts, no list.
  Nothing else changes.
- Why the class is honestly low: unchanged from `revisions/1.md`, now with the plan's proof attached: the
  code change is a constant, a `try`/`except` and a docstring sentence (plan step 2); the red-then-green
  commits are separable (`git diff` of the module between them is empty); the 94-test locked suite and
  defect 1's two modules stay green unmodified; the two real items that crashed are measured `CHAIN: PASS`
  with the token set before the pull request is marked ready. Up to the click the blast radius is nil; after
  it the change is the gate on `main`, which the owner reads as a two-line diff (the security-reviewer's
  correction on `revisions/1.md`, carried here).
- Remainder for a human: the click on #111, then the retire tap. No non-low work is left over and no
  `work/chain-check-without-gh-supervised` intent is opened. New at this gate: the park pull request
  (`claude/chain-check-without-gh-park`, ledger-only) is the agent's, merged by the delegated merge; the
  post-park merge of `origin/main` into the item branch is the agent's too, and it touches only `log.md`
  and the indexes.
- What goes stale: nothing in `spec.md` or `plan.md`. `revisions/1.md`'s "what goes stale" bullet predicted
  this record and the plan's shape; both are as predicted. The plan's step 4 says the mid-build
  `--diff origin/main` check, which will name the same path again, opens no third record: the route is
  already adopted at two gates and the rule's cap is two records per gate, not a record per run.

## Reviewer: plan-reviewer (claude-sonnet-5)
File list vs. route: `plan.md`'s `## Files that change` (plan.md:40-47) lists exactly the same seven paths
`revisions/2.md`'s route enumerates (revisions/2.md:32-38): `scripts/check_artifact_chain.py` (locked),
`scripts/test_chain_no_gh.py` (new), and the item's own `plan.md`/`log.md`/`revisions/2.md`/
`revisions/index.md`/`index.md`/`work/index.md`. No extra or missing path either direction. `python3
scripts/check_detour.py --slug chain-check-without-gh --plan work/chain-check-without-gh/plan.md`
reproduces the trigger verbatim: `scripts/check_artifact_chain.py: locked by locked-paths`, `DETOUR: needed
(1)`, exit 3 -- matching revisions/2.md's front-matter trigger.

Proof mapping: every spec row has a named test in plan.md's `## Proof` (plan.md:109-110):
R1→`test_a_token_with_no_gh_is_a_note_not_a_crash`,
R2→`test_a_token_with_no_gh_still_applies_the_author_rule_to_a_forged_trailer`,
R3→`test_the_retire_route_takes_the_same_path`, R4→`test_a_present_gh_that_fails_is_still_false` + the
94-test suite unmodified, R5→`test_the_check_never_reaches_for_the_network`,
R6→`test_a_stubbed_subprocess_still_reaches_the_call`, R7→step 4's five last lines. No gap.

Order of work for `kind: fix`: I reproduced the six cases against today's code directly (not just read it).
With `GH_TOKEN` set, `PATH` holding only a `git` shim, and a trailer commit built as
`ApprovalAuthor._reapprove_with_trailers` does it, the script crashes today with `FileNotFoundError: ...
'gh'` at `check_artifact_chain.py:237`, exit 1, no `CHAIN:` line -- R1/R2/R3 red as claimed, and R5's pin
holds even under that crash: `git status --porcelain` is byte-identical before/after and none of the 10
logged git argv lines contain a network verb. A `gh` shim that exits 1 already returns `(False, "...could
not be read...")` today (R4, confirmed by direct call). R6's stub-boundary technique is the same one
`DispatchAttestation._verify` (test_check_artifact_chain.py:657-687) already uses and is unaffected by a
missing `gh`. So step 1's "exactly three failures, three passes" is accurate, and the red/fix commits are
separable exactly as claimed (module untouched between them, only `check_artifact_chain.py` changes).

Park mechanics: `delegated_merge.check_pull_request` (delegated_merge.py:411-441) filters open pull
requests by matching `head_sha`, not by counting all open PRs, so PR #111 (code, refused by
`check_locked_paths` on `scripts/check_artifact_chain.py`, which is on `.sdlc/delegation.yaml:64`) and the
ledger-only park PR can coexist as the record claims. The park PR only touches `log.md` + regenerated
indexes, none of which are in `_locked_paths` (delegated_merge.py:386-395) or `intent.md`, so the delegated
merge can take it; its `advance()` clears `.sdlc/active` on the empty queue as
`knowledge/decisions/risk-detour.md` decision 5 anticipates. Plan step 5 explicitly names the one real
ordering hazard -- the park branch off `origin/main` and the `#111` branch will each append different
`log.md` lines, so the merge-back is a real conflict -- and gives the resolution ("keep both sets of lines,
the park line last"), so this isn't a hidden hazard, it's handled. (Separately, `origin/main` today is one
commit behind even the spec gate's own PR #110, so the park branch would also lack the spec-gate ledger
lines at merge time; the plan's generic resolution covers that too, and which lines are missing at merge
time is a fact about when PR #110 lands, not a defect in the route.)

Staleness/remainder: plan.md correctly implements revisions/1.md's plan-reviewer correction -- the click is
stated in prose above the file list (plan.md:30-37) and `## Release-gated` reads `(none)` with the click
explained as the locked-paths route, not a release gate (plan.md:49-51) -- so nothing predicted stale was
left wrong. The mid-build `--diff origin/main` re-trigger in step 4 correctly opens no third record, since
the detour rule's "new record" trigger is a changed route, not a repeat of the same trigger, and the route
is already adopted at both prior gates.

No gap found in file list vs. route, spec-to-test mapping, order-of-work soundness, or park mechanics.

*Writer's note, verbatim record.* The report above first ended with the word `keep` on its verdict line.
Since the template defines that word as a rejection of the route and the report finds no fault, the writer
asked the reviewer, in one message, which template meaning it intended, quoting both definitions and
saying the original word, the question and the answer would all be recorded. The reviewer's answer:
"Correction to my prior report: I intended to approve the route (no fault found in file list vs. route,
spec-to-test mapping, order-of-work soundness, or park mechanics), which under
docs/sdlc/templates/revision.md and the detour rule's definitions is `verdict: revise` (route sound, amend
and re-sign), not `keep` (reject, close, stop). My original word was a mistake in terminology, not a
change in assessment." Its final line follows.

verdict: revise

## Reviewer: security-reviewer (claude-opus-5)
Trigger reproduced independently: `python3 scripts/check_detour.py --slug chain-check-without-gh --plan
work/chain-check-without-gh/plan.md` prints `scripts/check_artifact_chain.py: locked by locked-paths
'scripts/check_artifact_chain.py'` / `DETOUR: needed (1)`, exit 3 on `4ba5de7` -- one path, and `## Files
that change` (`plan.md:40-47`) names no other `scripts/` entry but the new module, so the record's "nothing
else under `scripts/`" is true as printed.

The code change stays inside what the spec signed and leaves the pull-request-51 property intact.
`plan.md:41` is `spec.md:52-60` verbatim: the constant beside `DISPATCH_WORKFLOW_PATH`
(`check_artifact_chain.py:130`), `except FileNotFoundError` around the call at `:237`, one docstring
sentence, nothing before the call. Catching that one exception cannot turn a contradicting answer into a
skip -- `:239-240`'s `False` is untouched (R4/D2) -- and the returned tuple has `:233`'s shape, so the
caller is unedited and `:928` still FAILs an `ok is None` artifact whose approval commit is agent-authored,
identically to `:940`'s no-trailer FAIL. An unverifiable trailer buys an agent nothing, and an agent able to
strip `gh` from `PATH` can equally unset the token at `:232`: the delta is zero. Nit:
`subprocess.run(..., cwd=ROOT)` also raises `FileNotFoundError` when `ROOT` is gone, so the new note covers
one non-`gh` cause; no exploit (the `git` reads at `:200` fail first), but the docstring sentence should not
read as if the exception can only mean an absent binary.

Park mechanics hold on two independent refusals, and #111 cannot be taken by mistake. `check_pull_request`
(`delegated_merge.py:419-423`) keeps only open pull requests whose head sha equals the workflow_run's
`head_sha` (`:894`, read from the event, never from the head checkout) and refuses unless exactly one
matches; the park branch and #111 are different commits, so a run fired on the park head can see only the
park pull request. The slug binding (`:435-437`) does not discriminate -- both bodies carry `Work-Item:
chain-check-without-gh` -- the sha does; and were a run ever to fire on #111, `check_locked_paths`
(`:663-683` with `.sdlc/delegation.yaml:64`) refuses it, as would the pointer the park's own advance emptied
(`:435-437`). Smuggling a locked path into the ledger-only pull request fails the same way: `:922`/`:961`
fetch that number's file list through `gh_api`, which adds `--paginate` on GET (`:173-174`) so the list
cannot be truncated past a hidden entry, and `previous_filename` is matched too (`:678`). A `scripts/` file
is not itself locked, but the park branch is cut from `origin/main`, where
`work/chain-check-without-gh/plan.md` does not exist, and `require-plan.sh:20` blocks every `scripts/` write
with no plan in the checkout.

The file list is a closed contract: `check_artifact_chain.py:952-960` FAILs any changed path outside
`EXEMPT` (`:55` -- `work/ docs/ monitoring/ knowledge/ CLAUDE.md REVIEW.md README.md`; `evals/` survives in
the `:23` docstring only, so eval cases are in fact inside the contract) that matches no glob in the plan's
list. `protect-tests.sh:19,29-30` locks every existing `test_*.py` and `evals/cases/*`
(`.sdlc/config.env:19`) for the life of this `kind: fix` plan, which is what R4's "94 green unmodified"
rests on. Nit with a policy line: that same `:29` blocks edits to `scripts/test_chain_no_gh.py` the moment
it exists, so step 5's "fix and repeat" (`plan.md:75-77`) cannot revise the module after step 1 commits it
-- expect a hook refusal there, not a rewrite.

Rule 8: `log.md:19` now names the writer (`the writer is claude-fable-5-1`) beside both reviewer models,
closing revisions/1.md's gap; `.sdlc/delegation.yaml:39,43` requires the two sections present here, both
distinct from the writer, and this reviewer is read-only. Residual, minor: the plan-gate ledger line must
carry the same three names.

Rules: 1 -- `require-plan.sh:26-33` opens `scripts/` only on a plan signed `delegated` whose intent is
human-approved with `mode: delegated`; `plan.md:8` is still `in-review`, so the gate is shut until
`sign.py` writes. 2 -- the empty deviation bullet is in place (`plan.md:122-123`) and the CI contract is
`:952-960` above. 3 n/a -- nothing under `.sdlc/`, `.claude/hooks/` or `.github/workflows/` is in the
list, and the record proposes no unlock. 4 n/a -- the merge is the owner's click; the park pull request is
merged by the workflow, not by the session. 5 -- `VERIFY: PASS` owed by plan steps 3-4 before ready
(`plan.md:64-74`). 6 -- every finding above is `file:line`. 7 n/a -- no repeated mistake; revisions/1.md's
two corrections are carried at `revisions/2.md:43-45` and `log.md:19`.
security-standards: §1 n/a -- the note names the binary, the token is read by name at `:232` and never
printed, and the fixture's `GH_TOKEN=t` is a literal. §2 -- the attestation is the auth surface and `:928`
keeps the author rule beneath every `None`. §3 n/a -- `:237` is an argv list, no shell, no new input. §4
n/a -- no fields, no personal or regulated data. §5 n/a -- the change removes an implicit dependency on
`gh` being installed. §6 n/a. §7 -- one `note:` per artifact at `:925`, correlated by
`work/<slug>/<artifact>`; `:240`'s 200-char slice of `gh` stderr is unchanged. §8 -- above.

Human approver required: no (RELEASE_GATED_PATHS touched: none -- `migrations infra terraform helm`,
`.sdlc/config.env:12`; the owner's click on #111 is still owed under `locked-paths`,
`.sdlc/delegation.yaml:64`)
verdict: revise

## Closing note
`scripts/sign.py --revision` re-signs the artifact only when every `## Reviewer:` section above ends
`verdict: revise` and there are at least as many sections as the policy's `min-reviewers`. A `keep`
verdict, on any reviewer, or too few sections, means the agent does not re-sign: it stops here and
calls the owner back instead of proceeding on a split or insufficient review. On a detour record a
split closes the record; a changed route is a new record, and the second closed record at one gate
parks the item (`.claude/skills/sdlc-run/SKILL.md`, "The detour rule").
