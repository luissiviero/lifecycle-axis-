---
type: sdlc/plan
id: delegation-boundary
title: Decide whether a subagent may ever write code in this kit
description: "Files, order, proof and risks for the one-writer-until-ledger decision record, the four pointer lines, the two rule sentences, the index entries and the identity-blindness test."
stage: build
status: approved
kind: feature
reads: spec.md
approved-by: luissiviero
approved-on: 2026-09-05
risk-class: low
record:
resource: https://claude.ai/code/session_01Te8oN2GdvRupSixH4kjR8Y
tags: [agents, delegation, build, cost, spikes, decision, consensus-item-9]
timestamp: 2026-09-05T03:39:55Z
---
# Plan: decide whether a subagent may ever write code in this kit (from intent.md 2026-09-03)

## Files that change
- knowledge/decisions/one-writer-until-ledger.md — new; the decision record (spec R-1)
- knowledge/decisions/index.md — this record and the missing `adopt-script.md` entry (R-5)
- docs/sdlc/spikes/prompt-surfaces.md — one "overruled provisionally" line in §2.6 (R-2)
- docs/sdlc/spikes/build-stage-from-claude-agents.md — one "confirmed provisionally" line after the §2 table and one in §3.1 (R-2)
- docs/sdlc/phase-2-roadmap.md — one sentence in item 1b (R-3)
- docs/sdlc/rules/40-claude-only.md — one sentence on the subagents bullet (R-4)
- docs/sdlc/rules/50-gemini-only.md — the same sentence on its subagents bullet (R-4)
- CLAUDE.md — regenerated by `gen_context_files.py`; one line changes
- GEMINI.md — regenerated; one line changes
- AGENTS.md — regenerated; expected byte-identical
- scripts/test_hooks_baseline.py — `test_verdict_ignores_caller_identity_fields` in `ProtectPathsHook` and `ProtectApprovalsHook` (R-7)
- .sdlc/active — set to `delegation-boundary` by the session after the plan is approved (the owner approves from a phone and cannot run the activation from a shell)
- work/delegation-boundary/intent.md — the owner's draft: four answers proposed, `status: in-review`
- work/delegation-boundary/spec.md — new; this item
- work/delegation-boundary/plan.md — new; this item
- work/delegation-boundary/log.md — gate ledger, lines appended
- work/delegation-boundary/index.md — generated by `gen_index.py`
- work/index.md — regenerated

## Release-gated
(none)

## Order of work (each step independently verifiable)
1. Preconditions: `.sdlc/active` reads `delegation-boundary`; re-read the four answers in `intent.md` as the owner approved them and carry any edit into the record's text; `python3 scripts/run_tests.py -p test_hooks_baseline.py` green on `main`; `git log -1 --format=%h -- .claude/skills` prints `60ab0f5`. A difference is recorded under Deviations before any edit.
2. Test first: the two `test_verdict_ignores_caller_identity_fields` cases; they pass on `main` (the hooks are already identity-blind) and are the regression pin; `python3 scripts/run_tests.py -p test_hooks_baseline.py` green.
3. `knowledge/decisions/one-writer-until-ledger.md` per the spec; `python3 scripts/check_okf.py` ends `0 warnings`; `grep -c '^## '` prints `5`.
4. The four pointer lines (two spikes, roadmap); `git diff origin/main -- docs/sdlc/spikes/ | grep -c '^-[^-]'` prints `0`.
5. The two rule sentences; `python3 scripts/gen_context_files.py`; `wc -l < CLAUDE.md` ≤ 120; `scripts/checks/context-drift.sh` passes; `AGENTS.md` unchanged in `git status`.
6. `knowledge/decisions/index.md`; R-5 and R-6 oracles print the expected counts and nothing, respectively.
7. `python3 scripts/gen_index.py`; `scripts/verify.sh`; chain check with `--slug delegation-boundary`; `scripts/run_evals.sh`; ledger line `PR #<n> | draft -> in-review`; PR `[delegation-boundary] …` with `Work-Item: delegation-boundary`, the last lines pasted, and a request for `control-plane-approved` (`.sdlc/active` is a control-plane path).

## Risks
- Risk: the owner edits an answer in `intent.md` after this plan was drafted and the record text drifts from it → mitigation: step 1 re-reads the approved intent; the record quotes the answers, not this plan.
- Risk: the pointer line in `prompt-surfaces.md` reads as editing an accepted spike → mitigation: additions only, verified by the R-2 oracle; the spike's status and text stay.
- Risk: `CLAUDE.md` line budget → mitigation: one line; measured in step 5.
- What this could break: nothing executable; two context files gain a sentence.
- Options considered and not taken: deciding for the `implementer` role (no measurement on this kit supports it); a hedge ("delegate small steps") that neither spike would recognise, which the intent forbids; editing `.claude/agents/` (rule 3, and nothing to change); a live subagent measurement in this session (refused by the permission classifier, spec C2).

## Proof
- `scripts/verify.sh` green: last line `VERIFY: PASS (<sha>)`
- Spec rows → tests: R-1 → OKF `0 warnings`, the two `grep` oracles; R-2 → the additions-only oracle and the two counts; R-3 → the roadmap count; R-4 → the `CLAUDE.md`/`GEMINI.md` counts, `context-drift.sh`, `wc -l`; R-5 → the index count; R-6 → the agreement loop prints nothing; R-7 → `test_hooks_baseline.py::ProtectPathsHook::test_verdict_ignores_caller_identity_fields`, `::ProtectApprovalsHook::test_verdict_ignores_caller_identity_fields`; R-8 → `VERIFY: PASS`, `EVALS: N pass, 0 fail, N skipped`, `CHAIN: PASS`
- Manual / browser / screenshot / eval: `python3 scripts/run_tests.py` count grows by two over `main`.

## Rollback
- Revert the PR's commit; the two spikes return to contradicting each other, which is the state this item removes.

## Deviations log (append during implementation; same commit as the deviation)
- 
