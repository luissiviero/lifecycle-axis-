---
type: sdlc/plan
id: docs-reconcile
title: The docs say what the code does
description: "Files, order, proof and risks for the twelve rewrites, the front-matter checker and its wrapper, the timestamp pass, the eight lesson files and their fragment, the roadmap marks and the .gitattributes rule."
stage: build
status: approved
kind: feature
reads: spec.md
approved-by: luissiviero
approved-on: 2026-09-05
risk-class: low
record:
resource: https://claude.ai/code/session_01Te8oN2GdvRupSixH4kjR8Y
tags: [docs, okf, knowledge, lessons, reconcile, consensus-item-10, consensus-item-11]
timestamp: 2026-09-05T04:25:00Z
---
# Plan: the docs say what the code does (from intent.md 2026-09-05)

## Files that change
- scripts/check_front_matter.py — new; strict front-matter checker with structural fallback (spec D2)
- scripts/test_check_front_matter.py — new; six tests (spec D4)
- scripts/checks/front-matter.sh — new; self-registering wrapper (control plane)
- scripts/check_artifact_chain.py — docstring only: `## Files` → `## Files that change` (D1, B12-14)
- docs/sdlc/README.md — rows 27, 35, 37, 59, 103, 112, 147 and the "Verified before done" row (D1)
- docs/sdlc/metrics.md — line 11-12 rewrite; quote the title (D1, D2)
- docs/sdlc/phase-2-roadmap.md — done marks on items 12 and 13; quote the title (D1, D2)
- docs/sdlc/rules/index.md — order key and marker text (D1); entry for the new fragment
- docs/sdlc/rules/60-lessons.md — new fragment; heading, rule line, eight pointer lines (D4)
- docs/sdlc/lessons.md — Markdown link (R-3)
- docs/sdlc/okf-pairing.md — the freeze sentence (D3)
- docs/sdlc/spikes/gemini-parity.md — one line replaced at :84 (D1)
- docs/sdlc/handoff/HANDOFF.md — quote the description; timestamp (D2, D3)
- docs/sdlc/handoff/PLAN.md — quote title and description; timestamp (D2, D3)
- knowledge/decisions/deploy-from-ci.md — :39-40 and :61-62 (D1)
- knowledge/decisions/merge-click-is-the-gate.md — quote the description (D2)
- knowledge/decisions/one-rule-source.md — the lessons follow-up marked done (R-7)
- knowledge/metrics/post-deploy-5xx-rate.md — :42 (D1)
- knowledge/lessons/index.md — naming rule and eight entries (R-3, D4)
- knowledge/lessons/control-plane-unlock-is-advisory.md — new (D4)
- knowledge/lessons/one-path-spelling-in-guards.md — new
- knowledge/lessons/test-lib-changes-from-a-second-shell.md — new
- knowledge/lessons/fold-crlf-before-comparing.md — new
- knowledge/lessons/skills-spell-template-headings.md — new
- knowledge/lessons/plan-bullets-start-with-the-path.md — new
- knowledge/lessons/ledger-slot-holds-status-only.md — new
- knowledge/lessons/send-ledger-lines-in-a-fenced-block.md — new
- .claude/skills/sdlc-incident/SKILL.md — lesson step; tier wording (R-3)
- .gitattributes — four `text eol=lf` lines (R-8)
- CLAUDE.md — hand-kept lessons section removed; regenerated
- GEMINI.md — regenerated
- AGENTS.md — regenerated
- docs/** — timestamp rewrite only, on the files D3 lists (29 today; the exact list goes in the Deviations log)
- knowledge/** — same
- .sdlc/active — set to `docs-reconcile` by the session after the plan is approved (the owner approves from a phone)
- work/docs-reconcile/intent.md — new; this item
- work/docs-reconcile/spec.md — new; this item
- work/docs-reconcile/plan.md — new; this item
- work/docs-reconcile/log.md — gate ledger, lines appended
- work/docs-reconcile/index.md — generated by `gen_index.py`
- work/index.md — regenerated

## Release-gated
(none)

## Order of work (each step independently verifiable)
1. Preconditions: `.sdlc/active` reads `docs-reconcile`; `python3 scripts/run_tests.py` green on `main` (count noted);
   `python3 -c 'import yaml'` succeeds here; `git log -1 --format=%h -- .claude/skills` prints `60ab0f5`;
   the five documents fail `yaml.safe_load` (D2). A difference is recorded under Deviations before any edit.
2. Test first: `scripts/test_check_front_matter.py` (six tests) red; `scripts/check_front_matter.py` until green;
   `scripts/checks/front-matter.sh`; `python3 scripts/check_front_matter.py` on the branch prints the five documents and
   exits 1; quote the five; it prints `FRONT-MATTER: N docs, 0 problems (PyYAML)`.
3. The twelve rewrites of D1 plus the `check_artifact_chain.py` docstring; every D1 oracle prints its count; the R-2
   `git diff --stat` on the fifteen fixed locations is empty; spikes additions-only count prints `1`.
4. R-3: `lessons.md` link, `knowledge/lessons/index.md`, `/sdlc-incident` step and tiers; the four R-3 greps.
5. D4: eight lesson files; `docs/sdlc/rules/60-lessons.md`; `rules/index.md` entry; remove `CLAUDE.md:72-81`;
   `python3 scripts/gen_context_files.py`; `wc -l < CLAUDE.md` ≤ 120; `scripts/checks/context-drift.sh`; the R-7
   greps print `8` for all three files; `one-rule-source.md` follow-up marked done; `knowledge/lessons/index.md` lists
   the eight; `python3 scripts/check_okf.py` ends `0 warnings`.
6. Roadmap done marks and `gemini-parity.md` line (D1); `okf-pairing.md` freeze sentence (D3); `.gitattributes` (R-8):
   `git check-attr eol` prints `eol: lf` five times.
7. Timestamps (D3): run the one-off script from the scratchpad (not committed) over `docs/**/*.md` and
   `knowledge/**/*.md`, excluding files edited in steps 3-6 (those get the edit instant); paste the list of rewritten
   paths into the Deviations log; the D3 oracle ends `TIMESTAMPS: stale=0`; the index oracle prints nothing.
8. `python3 scripts/gen_index.py`; `scripts/verify.sh`; `python3 scripts/check_artifact_chain.py --base origin/main
   --slug docs-reconcile`; `scripts/run_evals.sh`; `python3 scripts/run_tests.py` grows by six; ledger line
   `PR #<n> | draft -> in-review`; PR body with the last lines, the C3 and C4 proposals, and the request for
   `control-plane-approved` (`scripts/checks/front-matter.sh`, `.sdlc/active`).

## Risks
- Risk: a timestamp rewrite lands in the same PR as a text edit and the two disagree → mitigation: step 7 runs after
  every edit and skips edited files, which carry the edit instant; the oracle tolerates 24 h.
- Risk: a lesson pointer line pushes `CLAUDE.md` past 120 → mitigation: one clause per line; measured in step 5.
- Risk: `yaml.safe_load` rejects a value the kit's parser accepted beyond the five (e.g. a bare `@handle`) → mitigation:
  step 1 runs the checker over every tracked `.md` before any edit; anything extra is quoted and listed under Deviations.
- Risk: the adopter's seeded `## Lessons learned` heading collides with the fragment heading → mitigation: the fragment
  heading is `## Lessons (one file each in knowledge/lessons/)` (spec C2).
- What this could break: `verify.sh` gains a check that can fail on a new document; nothing else is executable.
- Options considered and not taken: a CI timestamp check (OKF frozen, consensus item 10); grouping the lessons (intent
  Q2); editing `.sdlc/approvers.yaml` or `sdlc-gate.yml` (rule 3; proposals in the PR body).

## Proof
- `scripts/verify.sh` green: last line `VERIFY: PASS (<sha>)`
- Spec rows → tests: R-1 → the D1 oracles; R-2 → `git diff --stat` empty on the fixed locations; R-3 → the four greps;
  R-4 → `TIMESTAMPS: stale=0`, the `okf-pairing` grep; R-5 → the index oracle prints nothing; R-6 →
  `scripts/test_check_front_matter.py` (6), `FRONT-MATTER: N docs, 0 problems`, `front-matter.sh ✔ pass`, the
  `git show origin/main:docs/sdlc/metrics.md | … --stdin` exit 1; R-7 → the `ls`/`grep -c`/`wc -l`/`context-drift.sh`
  set; R-8 → `git check-attr`; R-9 → `VERIFY: PASS`, `CHAIN: PASS`, `EVALS: N pass, 0 fail, N skipped`, `OKF: N docs, 0 warnings`
- Manual / browser / screenshot / eval: open `docs/sdlc/metrics.md` and `knowledge/decisions/merge-click-is-the-gate.md`
  on GitHub after merge: the front-matter table renders, no YAML banner.

## Rollback
- Revert the PR's commit; the docs return to the B12 state, `verify.sh` loses the front-matter check.

## Deviations log (append during implementation; same commit as the deviation)
- 
