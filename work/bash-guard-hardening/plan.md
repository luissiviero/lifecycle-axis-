---
type: sdlc/plan
id: bash-guard-hardening
title: Bounded hardening of bash_write_targets, cwd-aware candidates, NotebookEdit coverage
description: Files, order of work, proof and risks for closing the verified Bash write-guard gaps within a 140-line bound; design and test strings live in spec.md.
stage: build
status: in-review
kind: feature
reads: spec.md
approved-by:
approved-on:
risk-class: low
record:
resource: https://claude.ai/code/session_01DSYmeD7eng2GKQ3EbHQDzo
tags: [hooks, bash-guard, control-plane, security, consensus-item-3]
timestamp: 2026-09-04T21:48:41Z
---
# Plan: bounded hardening of bash_write_targets, cwd-aware candidates, NotebookEdit coverage (from intent.md 2026-09-04)

## Files that change
- .claude/hooks/_lib.sh — `FILE` reads `notebook_path`; new `CWD`, `CWD_CANON`, `rel()` with a base; `bash_write_targets` steps 1, 2, 4 and the new arms; `bash_write_candidates` resolves against `CWD` (R-1..R-7, R-11)
- .claude/hooks/protect-paths.sh — line 36 `canon "$FILE"` becomes `rel "$FILE"` (R-7, R-8)
- .claude/hooks/block-secrets.sh — line 6 appends `.tool_input.new_source` (R-8)
- scripts/fixtures/hook_inputs/notebookedit.json (new) — NotebookEdit payload shape with a `_note` like `multiedit.json`'s (R-8)
- scripts/test_protect_paths_bash.py — `bash()` gains `cwd=`; the A4 block and allow lists; notebook `new_source` case (R-1..R-7, R-8)
- scripts/test_hooks_baseline.py — `ProtectPathsHook.test_blocks_notebook_under_sdlc` (R-8)
- scripts/test_bash_plan_gates.py — `ProtectTestsBashBranch` delete/rename cases under `kind: fix` (R-9)
- evals/cases/hook-blocks-delete-and-glued-writes.yaml (new) — runtime oracle for `rm`, glued `;`, `2>` (R-10)
- evals/cases/hook-blocks-bash-write-to-protected-path.yaml — adds the newline-separated form (R-10)
- knowledge/decisions/bash-write-guard.md — reword lines 99-102, replace 104-119 with `## Accepted residuals` (R-12)
- work/bash-guard-hardening/intent.md — this item's intent
- work/bash-guard-hardening/spec.md — this item's spec
- work/bash-guard-hardening/plan.md — this file; deviations log
- work/bash-guard-hardening/log.md — gate entries
- work/bash-guard-hardening/index.md — regenerated by `scripts/gen_index.py`
- work/index.md — regenerated by `scripts/gen_index.py`
- .sdlc/active — set to `bash-guard-hardening` by the owner's `approve.py --activate` on this branch

## Release-gated
(none)

## Order of work (each step independently verifiable)
1. Fixture and tests first, red: add `scripts/fixtures/hook_inputs/notebookedit.json`, the `cwd=` builder and every R-1..R-9 test; `python3 scripts/run_tests.py -p test_protect_paths_bash.py` and `-p test_bash_plan_gates.py` fail on exactly the new cases, the existing ones stay green.
2. `block-secrets.sh:6` `new_source`; `python3 scripts/run_tests.py -p test_protect_paths_bash.py` turns `BlockSecretsWiderSurface` green.
3. `_lib.sh`, one Write: `FILE` with `notebook_path`, `CWD`, `CWD_CANON`, `rel()` base, step 1 redirections, step 2 pre-split and separator lists (`:120` and `:129`), the new arms, step 4 rewrites, `bash_write_candidates` with `base`. Then `bash -n .claude/hooks/_lib.sh` and, from a second shell, `python3 scripts/run_tests.py -p 'test_*hook*.py' -p test_protect_paths_bash.py -p test_bash_plan_gates.py -p test_control_plane_hardening.py`; only then does this session issue its next tool call.
4. `protect-paths.sh:36` to `rel "$FILE"`; `test_hooks_baseline.py` and `test_protect_paths_bash.py` green, including `test_blocks_relative_write_when_cwd_is_sdlc`.
5. `awk '/^bash_write_targets\(\) \{/,/^\}/' .claude/hooks/_lib.sh | wc -l` prints at most 140; if not, drop `chgrp` then `rmdir` per spec Q2 and note it in the deviations log.
6. Evals: new `hook-blocks-delete-and-glued-writes.yaml`, extend `hook-blocks-bash-write-to-protected-path.yaml`; `scripts/run_evals.sh --only 'hook-blocks-*'` ends `0 fail`.
7. `knowledge/decisions/bash-write-guard.md`: reword `:99-102`, replace `:104-119` with `## Accepted residuals`; `python3 scripts/check_okf.py` ends `0 warnings`.
8. `python3 scripts/gen_context_files.py && python3 scripts/gen_index.py`; `scripts/verify.sh` ends `VERIFY: PASS (<sha>)`; `python3 scripts/check_artifact_chain.py --base origin/main --slug bash-guard-hardening` ends `CHAIN: PASS`; append gate entries to `log.md`; paste the last lines in the PR body with `Work-Item: bash-guard-hardening`; ask the owner for `control-plane-approved`.

## Risks
- Risk: a syntax or logic error in `_lib.sh` locks Edit, Write and Bash in the session that made it → mitigation: step 3 is one Write, `bash -n`, then the hook test modules from a second shell before the session's next tool call (CLAUDE.md lessons learned); the branch is restored with `git checkout -- .claude/hooks/_lib.sh` from that shell if red.
- Risk: the arms overflow the `~140 lines` ceiling for `bash_write_targets` (102 lines today at `_lib.sh:98-199`) → mitigation: step 5 measures; drop `chgrp` then `rmdir` (spec Q2) and record it in the deviations log.
- Risk: the pre-split changes a verdict an existing test pins (`echo "a > b"`, `scripts/verify.sh 2>&1 | tail -3`, `cp ${SRC} .sdlc/x`) → mitigation: those strings stay in the suites; step 1 runs them red-then-green around step 3; braces split only where the shell requires blanks (spec G3).
- Risk: `cwd` absent from a real payload → mitigation: fallback to ROOT is today's behaviour; `test_allows_relative_write_when_cwd_is_tmp` and the no-`cwd` block list cover both.
- Risk: this item lands after WI-2 and WI-3, which also touch `_lib.sh` → mitigation: serial landing, merge `main` first, re-run step 3's second-shell suite; helper names from earlier items are not used here.
- What this could break: nothing outside this repo; ordinary `rm`/`chmod` inside `PLAN_REQUIRED_PATHS` now needs the approved plan (spec C2).
- Options considered and not taken: a real shell parser (cost, and the residual list would still exist); resolving `cwd` in each hook separately (three copies of one rule); dropping the 140-line bound.

## Proof
- `scripts/verify.sh` green → `VERIFY: PASS (<sha>)`
- Spec rows → tests: R-1 → `test_protect_paths_bash.py` redirection block/allow cases; R-2 → its separator cases; R-3 → its `rm`/`unlink`/`rmdir`/`chmod`/`chown` cases; R-4 → its `mv`/`-t`/`--target-directory`/`sort -o` cases; R-5 → its `sed --in-place`/`perl -pi` cases; R-6 → its `git` cases; R-7 → its `cwd=`/`$PWD` cases; R-8 → `test_hooks_baseline.py::ProtectPathsHook.test_blocks_notebook_under_sdlc` and `BlockSecretsWiderSurface.test_blocks_key_in_notebook_new_source`; R-9 → `test_bash_plan_gates.py::ProtectTestsBashBranch` delete/rename cases; R-10 → `scripts/run_evals.sh --only 'hook-blocks-*'` ends `0 fail`; R-11 → the `awk … | wc -l` line prints at most 140 and `python3 scripts/run_tests.py` ends `OK`; R-12 → `grep -c '^## Accepted residuals' knowledge/decisions/bash-write-guard.md` prints 1 and `python3 scripts/check_okf.py` ends `0 warnings`
- Manual / browser / screenshot / eval: from a second shell, the 30 block strings piped as JSON into `protect-paths.sh` all print a block line; `python3 scripts/check_artifact_chain.py --base origin/main --slug bash-guard-hardening` ends `CHAIN: PASS`.

## Rollback
- Revert the PR's commits; the hooks return to the previous `_lib.sh`, and no state outside the repo changes. If `_lib.sh` is broken mid-implementation, `git checkout -- .claude/hooks/_lib.sh` from the second shell.

## Deviations log (append during implementation; same commit as the deviation)
- 
