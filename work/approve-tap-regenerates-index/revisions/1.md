---
type: sdlc/revision
id: approve-tap-regenerates-index-revision-1
title: A tap heals every index only on the default branch; elsewhere it writes the item's own two, and a rename's source is judged too
description: "The signed R-2 has the tap commit every regenerated index on whatever ref it runs on, but check_artifact_chain.py counts only work/index.md and work/<slug>/ as the item's own files, so a supervised tap on a work branch that heals another item's index turns that branch's pull request red in strict mode; and the guard judges only a rename's destination, so a staged rename can delete a path outside the allowlist under the approver's authorship."
artifact: spec.md, plan.md
trigger: "Security pass on pull request 83 (98daa7c), Blocking: spec.md R-2 (spec.md:28) and D2 make the committer stage work/<other>/index.md whenever it is stale, on any ref. check_artifact_chain.py:645-648 `own_artifact` returns True only for work/index.md, work/<slug>/... and .sdlc/active naming the slug; any other path in the diff selects strict mode (:650), which requires the whole chain approved. Reproduced A/B on one fixture: the tap commit carrying work/other/index.md prints `FAIL: work/demo/spec.md is missing`, `FAIL: work/demo/plan.md is missing`, `CHAIN: FAIL`; the same commit with work/other/index.md restored prints `mode: in-progress`, `CHAIN: PASS`. Every supervised spec or plan approval is a tap on a work branch (approve.yml writes the ref selected in the Actions tab), and knowledge/lessons/human-commits-leave-indexes-stale.md records a stale foreign index on main as the normal state after a retirement, so the signed design turns the very pull request being approved red, with errors naming the wrong files. Major: approve_dispatch.py:156-157 keeps only the destination of a porcelain rename entry (`R  old -> new`), so `R  .github/workflows/approve.yml -> work/a/index.md` staged before --commit yields changed_paths ['work/a/index.md'], unexpected_paths [] and rc 0, and HEAD no longer has the workflow file (reproduced end to end on a fixture; not reachable from the workflow's own steps today, which stage nothing, but the widening to work/<dir>/index.md multiplies the landing spots and falsifies the comment at approve_dispatch.py:210-211)."
timestamp: 2026-09-14T03:05:00Z
---
# Revision 1: heal every index only on the default branch, write the item's own two elsewhere, and judge a rename's source

## Proposal
Five changes to `spec.md`, the first two clearing the trigger's Blocking and Major findings, the rest the
nits both reviewers raised against the same lines; and the matching changes to `plan.md`.

1. **R-2 becomes ref-scoped.** On the default branch the tap writes and commits every generated index it
   changed, as signed: a grant lands only there (`approve_dispatch.py:89-96`, R-8 of `approve-by-dispatch`),
   there is no pull request whose mode a foreign index could flip, and drift on `main` is exactly what
   the owner asked the tap to heal. On any other ref the tap writes only `work/<slug>/index.md` and
   `work/index.md`, the two paths `own_artifact` already counts as the item's own, and leaves a stale
   foreign index untouched and unstaged, reporting it on stdout so the run log says the branch carries
   drift the tap did not commit. The ref comes from `GITHUB_REF_NAME` and the default branch from the
   event payload at `GITHUB_EVENT_PATH` (`repository.default_branch`, the same field the workflow's role
   gate already reads as `github.event.repository.default_branch`); both are set by Actions on every run,
   so no workflow input, flag or step changes. When either is unknown, as in a local run or the test
   fixture without explicit values, the committer takes the narrow route: it never stages a foreign path
   without knowing it is on the default branch. `commit()` gains keyword arguments `ref` and
   `default_branch` so the suite can drive both routes; `main()` and the CLI are untouched.
2. **A rename is judged at both ends.** `changed_paths` returns the source and the destination of a
   porcelain `R`/`C` entry, so a source outside the allowlist is a stray and refuses the commit as any
   other stray does. Pinned by a case that stages a rename from a committed path outside `work/` to a
   generated-index path and asserts the source is named and `HEAD` unchanged.
3. **One `git status`, one list.** `unexpected_paths` takes the reported list from the caller, so the
   guard and the staging read the same run, which is what D5 and the comment already claimed.
4. **`regenerate` writes only what it may commit, and says what it skipped.** It takes a predicate for
   the paths it may write (the generated-index predicate on the default branch, the item's own two
   elsewhere) and reports the rendered paths whose bytes differ but which it left unwritten. This is also
   what closes the nit that a directory `INDEX_RE` rejects (a leading dot) had its index written and then
   refused as a stray naming a file nobody asked for.
5. **Three stdout shapes, all proven, and one refusal that names its cause.** `regenerated <n> index
   file(s): <sorted paths>`; `indexes already up to date`; `left <n> stale index file(s) unwritten on this
   ref: <sorted paths>`. The index-only refusal keeps its `nothing staged; approve.py wrote no change`
   text and appends `(only generated indexes changed: <paths>)`, so a run log can tell it from the
   original refusal. R-8 drops "precedes the refusal", which two separately captured streams cannot
   prove, and R-2's case asserts the exact regenerated line in a fixture where the generator's order
   (`work/index.md` last) differs from sorted order, which is what pins the sort.

`plan.md` follows: the file list gains `revisions/1.md` and `revisions/index.md`; the six new cases become
ten (`test_on_another_ref_only_the_items_indexes_are_written`, `test_unknown_ref_takes_the_narrow_route`,
`test_a_rename_source_is_judged_too`, `test_a_traversing_slug_is_refused_on_a_clean_tree`, the last pinning
the up-front `allowed_paths` call the build restored in f8b0e1d, whose operand order one reviewer found
untested); step 1's red set is restated for the cases that can be red against the current head; the proof
table and risks name the ref decision and its two sources.

This is the smallest change that clears the trigger: the alternative that keeps R-2 unconditional needs
`own_artifact` in `scripts/check_artifact_chain.py` to accept `work/*/index.md`, a `locked-paths` file this
item may not touch and whose one-item-per-pull-request rule exists for a reason; a workflow input for the
ref touches a protected file and ends the delegated queue. Not proposed: refusing on a side branch when a
foreign index is stale (fails the owner's tap on a branch whose drift is not the item's), or writing the
foreign index without staging it (the stray guard would then refuse it).

### Amendments after the security review (its section is below; the plan review read the text above)
- **The ref decision fails narrow, never raises.** The payload read is one function that returns the
  default branch or `None`: an unset or empty `GITHUB_EVENT_PATH`, an unreadable file, JSON that does not
  parse, a top level or `repository` that is not an object, or a `default_branch` that is not a non-empty
  string all return `None`, and `None` takes the narrow route. The only exceptions `commit()` still lets
  propagate are the tree's own (`OSError` from `regenerate`). Pinned by `test_a_malformed_event_payload_takes_the_narrow_route`
  beside `test_unknown_ref_takes_the_narrow_route`.
- **A tag named like the default branch is not the default branch.** The wide route also requires
  `GITHUB_REF_TYPE` to be `branch`; anything else is narrow.
- **Two lists, one status run.** `changed_paths` returns the judging list, both ends of a rename or copy
  entry, and beside it the staging list, one entry per porcelain line (the destination for a rename, whose
  source deletion git already holds in the index). Staging the vanished source would make `git add` exit
  128 (reproduced by the reviewer on a staged `work/demo/intent.md -> work/demo/spec.md`).
- **Two residuals stated, not fixed.** On a non-default ref the committer that runs is the branch's own
  copy of `approve_dispatch.py`, so the narrow route is a chain-check correctness guard, not a boundary
  against a hostile branch; the boundary is that a grant lands only on the default branch. And only
  `regenerate`'s writes are ref-scoped: `is_allowed` still accepts any generated index, so a foreign index
  dirtied by something other than `regenerate` would be staged on a side branch; safe today because the
  checkout is fresh and the only earlier writer, the approval script, is byte-identical to `main`
  (`approve-by-dispatch` decision 3). Both go under Areas of concern.
- **The spec's "no new environment variable" line is amended, not restated**: the committer reads
  `GITHUB_REF_NAME`, `GITHUB_REF_TYPE` and `GITHUB_EVENT_PATH`, all runner-set, none caller-controlled.

### Amendments after the plan review (its section is below; it read the text above the first amendments)
- **One spelling of "which ref", threaded from the CLI.** `main()` passes the already-parsed `--ref` and
  `--default-branch` into `commit()`, and `commit()` falls back to the environment only when a value was
  not given, so `--commit --ref X` is never silently ignored. Pinned by
  `test_cli_threads_ref_and_default_branch_into_commit`.
- **One default-branch predicate.** `is_default_branch(ref, default_branch)` accepts `main` and
  `refs/heads/main`, is used by `check_actor` (its existing comparison, extracted unchanged, pinned by the
  existing `Mode` cases) and by the ref decision (`knowledge/lessons/one-path-spelling-in-guards.md`).
- **The staging predicate is ref-scoped too.** `is_allowed` and `unexpected_paths` take the route: on the
  wide route any generated index passes, on the narrow route only the item's own two, so a foreign index
  dirtied by anything at all is a stray on a side branch. The residual the security review named is
  closed rather than stated; the other residual (the branch's own copy of the committer runs) stays a
  stated one. R-3's positive case runs on the wide route explicitly; a narrow-route case pins the stray.
- **The intent scoped outcome 2 to `main` in its own words** (`intent.md`, "When main already carries a
  stale index of another item"); the spec generalised it to any ref, which is where the defect entered.
  The revised R-2 quotes the intent.
- The new cases are thirteen, not ten: the six signed, plus `test_on_another_ref_only_the_items_indexes_are_written`,
  `test_a_foreign_index_is_a_stray_on_the_narrow_route`, `test_unknown_ref_takes_the_narrow_route`,
  `test_a_malformed_event_payload_takes_the_narrow_route`, `test_a_rename_source_is_judged_too`,
  `test_a_traversing_slug_is_refused_on_a_clean_tree` and `test_cli_threads_ref_and_default_branch_into_commit`;
  R-9's count becomes 26 before, 39 after.

## Closing note
Reviewer sections are appended below this note only as the reviewers return them, verbatim, each on a
different model from the writer; `scripts/sign.py --revision` re-signs only when every one ends
`verdict: revise` and there are at least `min-reviewers: 2` of them. A `keep` on either, or fewer than two,
means the agent does not re-sign and calls the owner back with this record. The security pass's Major
finding is folded into this same revision rather than deferred because it is the same surface as the
trigger: what the committer may stage under the approver's authorship. The plan pass's Important finding
(an unlogged spec body edit in the build commit) is not part of this record; it is a deviation entry,
logged as 2 of 5.

## Reviewer: security-reviewer (opus)

Both halves of the trigger reproduce, so the trigger does block the plan as written. **Blocking:** `/home/user/lifecycle-axis-/scripts/check_artifact_chain.py:645-648` — `own_artifact` returns True only for `work/index.md`, `work/<slug>/…` and `.sdlc/active` naming the slug; `in_progress = all(own_artifact(p) …)` at `:650` therefore goes False the moment a tap commit carries `work/<other>/index.md`, and `:693-694` then emits `work/<slug>/spec.md is missing` / `plan.md is missing` for a half-built chain. Since `approve.yml:52,126,131` runs and pushes on the ref selected in the Actions tab, every supervised spec/plan tap is a tap on a work branch, and `knowledge/lessons/human-commits-leave-indexes-stale.md` makes a stale foreign index the normal state — signed R-2 (`/home/user/lifecycle-axis-/work/approve-tap-regenerates-index/spec.md:28`) turns the very PR being approved red. **Major:** reproduced end-to-end on a scratch repo with `commit(..., identity="x <1+x@users.noreply.github.com>")`: with `.github/workflows/approve.yml` committed and then `git mv`'d to `work/a/index.md`, `git status --porcelain` prints `R  .github/workflows/approve.yml -> work/a/index.md`, `changed_paths` returns `['work/a/index.md', 'work/demo/intent.md', 'work/demo/log.md']` (the source is dropped at `/home/user/lifecycle-axis-/scripts/approve_dispatch.py:158-159`), `unexpected_paths` is `[]`, `commit()` returns 0, and `git show --name-status HEAD` is `D .github/workflows/approve.yml / A work/a/index.md …` under the approver's authorship — a control-plane deletion the guard never names, which falsifies the comment at `approve_dispatch.py:210-211` and the workflow header's D6 sentence (`approve.yml:19-20`). Security standards rule by rule against the *proposal*: §1 secrets — n/a, the dispatch event payload holds no secret and only `repository.default_branch` is read (`revisions/1.md:22-24`); §2 auth — n/a, `check_actor` (`approve_dispatch.py:89-110`) is untouched and the new ref logic scopes staging, not permission (note it introduces a *second* source for "which ref": a `--ref` flag at `approve.yml:96-98` versus an env read inside `commit()`; both derive from `github.ref_name`, so they cannot disagree today); §3 input — partially unmet, see below; §4 data — n/a, no new field; §5 dependencies — n/a, `json` is stdlib; §6 infra — n/a, `RELEASE_GATED_PATHS` is `migrations infra terraform helm` (`/home/user/lifecycle-axis-/.sdlc/config.env:15`) and nothing under it is touched, `approve.yml` stays byte-identical (that is precisely why the proposal reads env instead of adding a flag); §7 logging — n/a, the new stdout shapes and the refusal suffix (`revisions/1.md:40-43`) carry repo-relative paths only; §8 hygiene — the reviewers are read-only, but I am opus and, if the writing session was also opus, the record's own "different model from the writer" requirement (`spec.md:123-124`) is satisfied only by the second section.

On the smallest-change question the direction is right but the record is not yet safe to sign. Nothing a dispatch caller controls reaches either input: the four inputs (`approve.yml:24-43`) are choices/strings that feed step `env:` only, `GITHUB_REF_NAME` is set by the runner from the selector, and `GITHUB_EVENT_PATH` points outside the workspace, so head files cannot rewrite it — the one residual is that `ref_name` does not discriminate branch from tag, so a tag named `main` yields `main` (`[Nit][security] revisions/1.md:22-23`; the same imprecision already exists at `approve_dispatch.py:107`, and `GITHUB_REF`/`GITHUB_REF_TYPE` would cost one line). Failure direction for missing or empty values is correct and fail-closed (narrow route), but `revisions/1.md:26-27` says only "unknown", and malformed JSON, a non-dict `repository` or `"default_branch": null` would raise `ValueError`/`TypeError`/`AttributeError` out of `commit()` before any `git add` — nothing lands, but the approval dies with a traceback on a path `spec.md:92-99` currently claims can only be an `OSError` on the tree (`[Important][bugs] revisions/1.md:26-27` — name the except clause and add a malformed-payload case beside `test_unknown_ref_takes_the_narrow_route`). Judging both ends of a rename does close the demonstrated deletion (the source becomes a stray and refuses before staging), and predicate-gated writes widen nothing — they are load-bearing, not extra, because D5 stages whatever `git status` reported, so a foreign index written to disk would be committed regardless of the ref decision; but items 2 and 3 collide: with both ends in `changed_paths`, a rename whose two ends are *both* allowed makes `git add -- <source>` exit 128 (`fatal: pathspec 'work/demo/intent.md' did not match any files`, reproduced on a staged `work/demo/intent.md -> work/demo/spec.md`), which `git(..., check=True)` at `approve_dispatch.py:75-78` turns into a `SystemExit` mid-run (`[Important][bugs] revisions/1.md:31-34` — say that the two-end list is the *judging* list and the staging list keeps one entry per porcelain line, or stage with `git add -A --`). One residual to state in the record rather than fix: only `regenerate`'s writes are ref-scoped; `is_allowed` still accepts any generated index, so on a side branch a foreign index dirtied by anything other than `regenerate` would still be staged — safe today only because the checkout is fresh and `approve.py` is byte-identical to main (`spec.md:33`, decision 3). None of the four items exceeds the trigger; item 5's refusal suffix and stdout shapes are the cheapest way to keep the run log honest about the new narrow route. Note also that on a non-default ref the committer that runs *is the branch's own copy* of `approve_dispatch.py`, so this scoping is a chain-check correctness guard, not a boundary against a hostile branch — worth one sentence in the record so a later reader does not over-trust it. Stale if it lands: `spec.md:5` (description, "other items' included … heals drift already on main"), `:28` (R-2 row and its acceptance test), `:29` (R-3's positive half now needs the wide route and the narrow route needs its own row), `:31` (R-5's refusal text gains the suffix), `:34` (R-8's two stdout shapes become three; "precedes the refusal" dropped), `:35` (R-9: "six new cases", 26→32), `:43-48` (design step 1, now predicate-gated), `:56-60` (step 4), `:76-77` (`regenerate(root) -> list[str]` signature, and `commit()`'s new `ref`/`default_branch` kwargs are absent from Interfaces), `:78-81` ("unchanged text" becomes false), `:85` ("No new flag, input, **environment variable**…" is directly contradicted by reading `GITHUB_REF_NAME`/`GITHUB_EVENT_PATH`, and `revisions/1.md:24`'s "no workflow input, flag or step changes" elides that clause — fix the spec line, do not restate it), `:92-107` (failure modes: add the unknown-ref and malformed-payload routes; two predicates now exist), `:115-117` (§3 must name the env/JSON boundary), `:134-140` (C2's "a `contents: write` run now commits files derived from other items" becomes default-branch-only), `:152` and `:162-166` (D2), `:179-183` (D5 is no longer literally "stage what `git status` reported"), `:215-217` (G-8's claim that the heal removes the extra pull requests holds only on main); and `plan.md:4-5` (title/description), `:33-34` (file-list bullets, "six new cases"), `:38-39` (must gain `revisions/1.md` and `revisions/index.md` or CI fails the unplanned-file rule), `:47-56` (step 1's red set), `:57-66` (steps 2-3, "32 cases"), `:81-96` (risks: the ref decision's two sources, the malformed payload), `:110-118` (proof rows R-2/R-3/R-5/R-8/R-9 and the 26→32 count). Minor: `revisions/1.md:6` declares `artifact: spec.md` while the proposal also re-signs `plan.md`; `sign.py:71-99` and `check_artifact_chain.check_revisions` never cross-check that field, so a single `sign.py <slug> spec.md plan.md --revision revisions/1.md` works, but the record should say both. Human approver required: no (RELEASE_GATED_PATHS touched: none).
verdict: revise

## Reviewer: plan-reviewer (opus)

I reproduced the Blocking claim on my own fixture rather than trusting the trigger's. Scratch repo at `/tmp/claude-0/-home-user-lifecycle-axis-/0c406528-e916-512c-9e69-693a29ccb0ee/scratchpad/demo-repo`: `main` holds `.sdlc/active` = `demo`, an approved-shaped `work/demo/intent.md` (copied from `work/_example/intent.md`, id rewritten), `work/demo/log.md`, `work/other/intent.md`, and stale `work/demo/index.md`, `work/other/index.md`, `work/index.md`. Branch `tapA` commits `work/demo/{intent.md,log.md,index.md}`, `work/index.md` **and** `work/other/index.md`; branch `tapB` the same commit minus the foreign index. `GH_TOKEN= GITHUB_TOKEN= python3 /home/user/lifecycle-axis-/scripts/check_artifact_chain.py --base main --slug demo` from inside the repo gives, on tapA: `  FAIL: work/demo/spec.md is missing` / `  FAIL: work/demo/plan.md is missing` / `CHAIN: FAIL` (rc 1); on tapB: `  note: mode: in-progress -- the diff touches only work/demo/ (and .sdlc/active naming it), so the chain is checked as far as it exists; any other path in the diff needs the whole chain approved` / `CHAIN: PASS` (rc 0). The mechanism is `own_artifact` at `/home/user/lifecycle-axis-/scripts/check_artifact_chain.py:645-648` (only `work/index.md`, `work/<slug>/...`, `.sdlc/active`) feeding `in_progress = all(own_artifact(p) ...)` at `:650`. So one foreign index in the diff flips the mode and the errors then name files the pull request never had to have. This is not covered anywhere in the signed plan or spec, and it is not hypothetical for a *supervised* approval: `/home/user/lifecycle-axis-/.github/workflows/approve.yml:15-17` writes the ref selected in the Actions tab, its Commit step pushes `HEAD:$REF` (`:126-131`), and only a `mode: delegated` grant is pinned to the default branch (`approve_dispatch.py:102-109`, `approve.yml:33`) — so a spec or plan tap is by definition a tap on the work branch, where the item's own plan does not exist yet, i.e. exactly the tapA shape. `knowledge/lessons/human-commits-leave-indexes-stale.md:14-19` independently records the same collision ("a single one carrying both items' indexes is in-progress mode for neither slug and the chain check demands a full approved chain neither item had yet") and its rule line — "the regeneration must ride under the item whose index it is, because the chain check reads one work item at a time" — is precisely what signed R-2 (`work/approve-tap-regenerates-index/spec.md:28`) violates off the default branch. Decisively, the approved `intent.md:54-56` scopes outcome 2 to `main` in its own words ("When **main** already carries a stale index of another item... so the ref it writes to is `INDEX: up to date`"); the spec generalised "main" to "any ref", which is where the defect entered, so ref-scoping restores the intent rather than narrowing it. The trigger blocks; the plan does not already cover it.

On minimality: the proposal's route is close to the floor but not exactly it. The strictly smallest change that clears the trigger is to delete the foreign-index heal entirely (write only the item's two paths everywhere) — that needs no env reads, no new kwargs, no payload parsing — but it would throw away an owner-answered intent outcome, so I accept the ref-scoped route as the smallest change that clears the trigger *and* keeps the approved outcome. The `GITHUB_REF_NAME` + `GITHUB_EVENT_PATH` (`repository.default_branch`) route is forced, not chosen: the Commit step's env (`approve.yml:121-127`) carries `REF` but not `DEFAULT_BRANCH` (that appears only on the role-gate step, `approve.yml:92-93`), and adding it means editing a protected file; and `scripts/check_artifact_chain.py` is on `locked-paths` (`.sdlc/delegation.yaml:64`), so widening `own_artifact` really is closed to this delegated item. The narrow default when either value is unknown is right (it is also not a regression: before this item nothing regenerated at all), but three things must be nailed down in the revision or it re-opens the same class of bug: (a) `main()` should thread the already-parsed `--ref`/`--default-branch` (`approve_dispatch.py:247-248`) into `commit()` with the env as fallback — leaving the CLI "untouched" means `--commit --ref X` silently ignores `X`, a second spelling of the same question; (b) the default-branch comparison must be one shared predicate with `check_actor`'s at `approve_dispatch.py:107` (it accepts both `main` and `refs/heads/main`), per `knowledge/lessons/one-path-spelling-in-guards.md`; (c) an absent or malformed `GITHUB_EVENT_PATH` must fall to the narrow route, not raise — `regenerate` runs before any staging, so an uncaught `json` error would fail every tap, and the listed cases (`test_unknown_ref_takes_the_narrow_route`) should cover unreadable payload explicitly. I verified the Major finding end to end too: in a second scratch repo, `git mv .github/workflows/approve.yml work/a/index.md` yields porcelain `R  .github/workflows/approve.yml -> work/a/index.md`, and `changed_paths()` returns `['work/a/index.md']` with `unexpected_paths('a')` `[]` (`approve_dispatch.py:158-159`, the line the trigger cites as :156-157 against 98daa7c, which matches that commit). Folding it in is right — it is the same question (what may this commit carry) and the widening multiplies the destinations — but note the sibling gap the proposal does not close: ref-scoping only what `regenerate` *writes* leaves `is_allowed`/D5 (`approve_dispatch.py:131-136`, `:213-222`) willing to stage a foreign index on a side branch if anything else dirtied it, which holds today only because a runner checkout happens to be clean — the same "containment that holds because of what happens not to exist" the file rejects at `:51-56`. Scope the staging predicate by ref as well, or say in D-text why the write gate is sufficient. Nit 3 is real and worth folding: `commit()` runs `git status` twice (`:208` via `unexpected_paths`, `:215`) while the comment at `:213-214` claims one list drives both. What goes stale if this lands, by file:line — `spec.md:5` (description: "commits every generated index it changed... heals drift already on main" must become ref-scoped), `spec.md:28` (R-2 and its test), `spec.md:29` (R-3's positive case now needs an explicit ref), `spec.md:33` R-7 and `spec.md:35` R-9 (case count 26→32 becomes 26→36; `run_tests.py -p test_approve_dispatch.py` reports `Ran 32 tests ... OK` at HEAD), `spec.md:34` (R-8: drop "precedes the refusal", add the third stdout shape and the refusal suffix), `spec.md:39-41` (cites `approve_dispatch.py:141-164`; `commit()` is at `:204` at HEAD), `spec.md:43-60` (design steps 1, 3 and 4), `spec.md:68-79` (Interfaces: `regenerate` gains a predicate and a second return list, `unexpected_paths` gains the caller's list, stdout shapes), `spec.md:85` ("No new flag, input, **environment variable**, workflow step" — flatly false once the committer reads two), `spec.md:100-107` (failure modes: the rejected-index case is replaced by "left unwritten", and a payload-read failure mode is missing), `spec.md:134-140` (C2's "commits files derived from other items" is now default-branch-only), `spec.md:150-152` and `spec.md:162-166` (open-question answer and D2's consequence), `spec.md:214-217` (G-8's "the heal in R-2 is what removes that pair of pull requests from every future tap" — after ref-scoping it removes them only for taps on `main`; this is the item's headline benefit and must be restated honestly); `plan.md:4-5` and `plan.md:34` (six cases → ten), `plan.md:33` (the `approve_dispatch.py` bullet must name the ref decision, the rename source and the single `git status`), `plan.md:35-39` (Files that change must gain `work/approve-tap-regenerates-index/revisions/1.md` and `revisions/index.md`, or CI's rule-2 check fails the pull request; `revisions/index.md` is not a `gen_index` output — `render_all` emits only `work/<slug>/index.md` and `work/index.md`, `gen_index.py:238-248` — so it needs no allowlist change), `plan.md:50-56` (step 1's red set, already once wrong per deviation 1 of 5 at `plan.md:129-134`), `plan.md:65` (32 cases → 36), `plan.md:82-96` (risks: add ref/payload detection), `plan.md:91-93` (the "index the predicate rejects" risk is superseded by the write-predicate change), `plan.md:100-106` (options must record the `own_artifact` and workflow-input alternatives and why `locked-paths` closes them), `plan.md:110-118` (proof rows and the 26/32 count).
verdict: revise
