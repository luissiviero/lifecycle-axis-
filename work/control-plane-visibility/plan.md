---
type: sdlc/plan
id: control-plane-visibility
title: Hook decisions must be logged; the control-plane unlock must be visible; CI must recognise the kit's own agent PRs
description: Add log_decision and the A1 helpers to _lib.sh, a systemMessage and never-unlock list to protect-paths.sh, a prefix list and trailer scan to check_control_plane.sh, with tests, two evals and the doc corrections.
stage: build
status: approved
kind: feature
reads: spec.md
approved-by: luissiviero
approved-on: 2026-09-04
risk-class: low
record:
resource: https://claude.ai/code/session_01DSYmeD7eng2GKQ3EbHQDzo
tags: [hooks, control-plane, unlock, audit-log, ci, consensus-item-2, consensus-item-4]
timestamp: 2026-09-04T21:46:58Z
---
# Plan: hook decisions must be logged; the control-plane unlock must be visible; CI must recognise the kit's own agent PRs (from intent.md 2026-09-04)

## Files that change
- .claude/hooks/_lib.sh — CWD and SESSION_ID after line 50; DECISION_LOG, log_decision, logging block() and ask() after line 78; fm_value, artifact_role, approver_has_role (A1); FILE falls back to notebook_path (line 49); rel() resolves against CWD (line 75)
- .claude/hooks/protect-paths.sh — never-unlock case before the PROTECTED_PATHS test (A5); UNLOCKED accumulator and log_decision unlock in the unlock branch; finish() on both exit-0 paths (A2)
- scripts/check_control_plane.sh — AGENT_BRANCH_PREFIXES from config with default "claude/ kit/ spike/" (line 35); AGENT_TRAILER_RE, has_agent_trailer(), third disjunct at lines 76-79 (A2)
- .sdlc/config.env — new commented key AGENT_BRANCH_PREFIXES next to CONTROL_PLANE_LABEL
- .sdlc/README.md — line 1 says the unlock exists here and names the three never-unlock files; a line for AGENT_BRANCH_PREFIXES and one for hook-decisions.log
- .sdlc/active — point at this work item (--activate) during implementation
- .gitignore — add .sdlc/hook-decisions.log and monitoring/series/
- scripts/test_lib_helpers.py — new; R-1, R-2, R-8 helper tests through bash -c '. _lib.sh; ...' with crafted stdin
- scripts/test_bash_plan_gates.py — UnlockOnEditBranch gains the log and systemMessage cases; new classes DecisionLog and NeverUnlock (R-1 to R-5)
- scripts/test_check_control_plane.py — kit/ prefix, config prefixes, three trailer cases, base-only trailer (R-6, R-7)
- evals/cases/hook-unlock-writes-decision-log.yaml — new; R-3 oracle in a mktemp repo
- evals/cases/ci-control-plane-detects-agent-trailer.yaml — new; R-7 oracle in a mktemp repo
- knowledge/decisions/self-hooks-on.md — lines 55 and 59-61: the audit trail is the log plus systemMessage; PLAN_REQUIRED_PATHS is scripts
- knowledge/decisions/bash-write-guard.md — lines 51-56 (CI now sees prefixes and trailers) and 77-78 (stderr is not the transcript; the log is)
- knowledge/decisions/control-plane-label.md — lines 32-34: prefix list from config plus trailer scan; residual C3 and C4
- CLAUDE.md — lesson line 68 (outside the generated markers): audit line goes to the log and a systemMessage
- docs/sdlc/README.md — line 108: "agent-authored (branch prefix in AGENT_BRANCH_PREFIXES or a Claude trailer)"
- docs/sdlc/metrics.md — line 25: source is .sdlc/hook-decisions.log
- work/control-plane-visibility/log.md — gate entries
- work/control-plane-visibility/index.md — regenerated by scripts/gen_index.py
- work/index.md — regenerated by scripts/gen_index.py

## Release-gated
(none)

## Order of work (each step independently verifiable)
1. `.gitignore` and `.sdlc/config.env` key; `git check-ignore .sdlc/hook-decisions.log monitoring/series/x` prints both; `bash -n .sdlc/config.env`.
2. `scripts/check_control_plane.sh` plus its six new tests; `python3 scripts/run_tests.py -p test_check_control_plane.py` ends `OK`; `scripts/run_evals.sh --only 'ci-control-plane-*'` ends `0 fail` (existing eval still passes).
3. `scripts/test_lib_helpers.py` written first and red; then ONE Write to `.claude/hooks/_lib.sh`; from a second shell: `bash -n .claude/hooks/_lib.sh`, then `python3 scripts/run_tests.py -p 'test_*.py'` ends `OK` before this session issues another tool call.
4. New tests in `scripts/test_bash_plan_gates.py` (red), then `.claude/hooks/protect-paths.sh`; from the second shell `python3 scripts/run_tests.py -p test_bash_plan_gates.py -p test_protect_paths_bash.py -p test_control_plane_hardening.py -p test_hooks_baseline.py` ends `OK`.
5. The two eval cases; `scripts/run_evals.sh` ends `0 fail`.
6. Docs: the seven Markdown files above; `python3 scripts/check_okf.py` ends `0 warnings`; `wc -l CLAUDE.md` under 120.
7. `python3 scripts/gen_context_files.py && python3 scripts/gen_index.py`; `scripts/verify.sh` ends `VERIFY: PASS (<sha>)`; `python3 scripts/check_artifact_chain.py --base origin/main --slug control-plane-visibility` ends `CHAIN: PASS`; last lines pasted in the PR; the owner applies `control-plane-approved` (this PR trips the very check it adds).

## Risks
- Risk: a bad `_lib.sh` edit locks Edit, Write and Bash at once (CLAUDE.md line 70) → mitigation: step 3's one-Write rule, `bash -n`, and the full hook suite from a second shell before the next tool call.
- Risk: `finish()` prints JSON when nothing was unlocked and breaks `test_bash_plan_gates.py:176` (empty output) → mitigation: the guard `[ "${#UNLOCKED[@]}" -gt 0 ] || exit 0` is the first line; test pins it.
- Risk: the stderr unlock line changes by accident → mitigation: four existing assertions (`test_protect_paths_bash.py:170`, `test_bash_plan_gates.py:151,158`, `test_control_plane_hardening.py:111`) and eval `hook-unlock-covers-edit-branch` fail.
- Risk: a hook run inside a repo with no `.sdlc/` (adopter before `adopt.sh`) → mitigation: `log_decision` swallows the append error; R-2 test.
- Risk: the new eval appends to this repo's live log → mitigation: it runs in a `mktemp -d` repo (spec gotcha).
- Risk: `fm_value`, `artifact_role`, `approver_has_role` land with no caller and drift before WI-5/WI-6 use them → mitigation: `test_lib_helpers.py` pins the A1 contract now; WI-5/WI-6 plans name them under their own Risks.
- What this could break: nothing that passes today, by construction (additive helpers; the only new blocks are the three never-unlock paths, which no test or eval writes to under the unlock).
- Options considered and not taken: `permissionDecision: allow` with a reason (suppresses the tool's own prompt; rejected); moving the unlock to the launching shell (owner chose to keep it); a config key for the never-unlock list (a config line could then widen it).

## Proof
- `scripts/verify.sh` green
- Spec rows → tests: R-1 → `test_lib_helpers.py::test_log_decision_appends_six_tab_fields`, `test_bash_plan_gates.py::DecisionLog::test_block_is_logged`, `::test_ask_is_logged`; R-2 → `DecisionLog::test_read_only_sdlc_does_not_break_hook`, `test_lib_helpers.py::test_log_decision_failure_is_silent`; R-3 → `UnlockOnEditBranch::test_unlock_appends_log_line`, `::test_unlock_emits_one_system_message_for_two_targets`, `::test_unlock_leaves_unprotected_paths_silent`, eval `hook-unlock-writes-decision-log`; R-4 → `UnlockOnEditBranch::test_unlock_never_emits_permission_decision`; R-5 → `NeverUnlock` (four tests); R-6 → `test_check_control_plane.py::test_kit_branch_no_label_is_blocked`, `::test_prefixes_come_from_config_env`; R-7 → the four trailer tests and eval `ci-control-plane-detects-agent-trailer`; R-8 → the nine `test_lib_helpers.py` helper tests; R-9 → `python3 scripts/run_tests.py` ends `OK`, `scripts/run_evals.sh` ends `0 fail`, `git check-ignore` prints both paths.
- Manual / browser / screenshot / eval: after the session restart, one Edit under `.sdlc/` with the unlock; the owner confirms whether the systemMessage rendered and records it in the deviations log (C5); `tail -3 .sdlc/hook-decisions.log` shows the unlock line.

## Rollback
- Revert the PR: the log file is git-ignored and harmless; no data or schema to roll back. Restart the session after reverting `_lib.sh` or `protect-paths.sh`. The kit's own control-plane PRs return to the unchecked state until re-landed.

## Deviations log (append during implementation; same commit as the deviation)
- 2026-09-04: `DecisionLog::test_read_only_sdlc_does_not_break_hook` induces the failed append with a directory at the log path as well as `chmod 555`: the suite runs as root in the remote session and root ignores a read-only bit, so the chmod alone proved nothing.
- 2026-09-04: `scripts/test_lib_helpers.py` uses `bash -c` with `$0` set to a fake hook name so the log's hook field is testable; `test_check_control_plane.py::ControlPlaneRepo` gains `base_message` and `config_env` constructor arguments for the trailer-on-base and config-prefix cases (the plan named the tests, not the fixture changes).
- 2026-09-04: the pre-existing eval `hook-unlock-covers-edit-branch` runs the hook against this repo (`CLAUDE_PROJECT_DIR="$R"`), so every `scripts/run_evals.sh` now appends one `unlock` and one `block` line to this repo's git-ignored log; it is not in this item's file list and is left as it is (harmless, noted for WI-9 agent-evals).
- 2026-09-04: the step-3 "second shell" rule was met by one Bash command that copies the new `_lib.sh` into place, runs `bash -n`, a smoke invocation and the whole suite, and restores the previous file from git if any of them fails, so no later tool call could meet a broken library; the same shape was used for `protect-paths.sh`. Test count 318 -> 345.
- 2026-09-04: C5 (whether `systemMessage` renders for an exit-0 PreToolUse hook) stays open: the implementing session cannot see its own hook stdout; the owner checks once after restarting a session in this folder and records the answer here.
- 2026-09-04: three file-list bullets were written as `path (new) — note`; `check_artifact_chain.py` reads the bullet up to the first ` — ` as the path, so the chain check failed on `scripts/test_lib_helpers.py (new)` and the two eval cases after the first push (0976fc3). Rewritten as `path — new; note`. The same shape sits in the in-review plans of deploy-gate (6 bullets), loop-protection (2) and bash-guard-hardening (2); a CLAUDE.md lesson line records it (rule 7: the empty deviation bullet `- ` with a trailing space tripped WI-1's edit too).
\n- 2026-09-04: review nit on PR #26 (automated reviewer): `log_decision` folds tabs, CRs and newlines in the detail to spaces, so a multi-line `ask()` reason (a heredoc command from `production-gate.sh`) is one TSV row; `test_lib_helpers.py::DecisionLog::test_log_decision_folds_tabs_and_newlines_in_detail` pins it. The reviewer's Important finding (hook files without the implementation) was its sandbox: `review.yml` restores `.claude/` and `CLAUDE.md` from the base branch before the reviewer runs.\n