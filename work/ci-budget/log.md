---
type: sdlc/log
id: ci-budget-log
title: Gate ledger for ci-budget
description: Chronological record of stage transitions and approvals for this work item.
timestamp: 2026-09-09T09:10:00Z
---
# Log: ci-budget

Format: `- <RFC3339> | <artifact> | <from> -> <to> | <actor> | <sha> | <note>`
(append-only; `<note>` is optional; parsed by `scripts/log_ledger.py`).

- 2026-09-09T09:10:00Z | intent.md | (none) -> in-review | claude | 603695b | drafted from the owner's three goals (fewer GitHub minutes, less waiting, fewer wasted tokens; keep the crucial procedures, loosen the rest) and the Actions run history: the quota block at 2026-09-08 21:43 UTC after 1,124 runs, the approve tap that died in 3 seconds behind it, a 5m54s claude -p triage on every red gate (pull requests 59-61 each paid it for a drift already on main), the agent-evals PR job repeating what verify.sh ran, delegated-merge waking on a workflow the policy no longer requires, and 89 of 96 gate runs superseded by a later push; six questions answered as proposals for the owner to edit or accept
- 2026-09-09T09:40:00Z | intent.md | in-review -> in-review | claude | 2c16dae | open questions answered by the owner: skip the gate on drafts entirely; triage behind a `triage` label; "require branches to be up to date" off with "one agent code pull request open at a time" as the compensating control; the reviewer's model unchanged until measured; the review-comment question closed by reading the code (check_review already binds to the newest successful run, delegated_merge.py:605-610); the measure is a series named actions_minutes_per_pr. Two questions added and answered: the superseded-run rule in delegated_merge.py (a skipped or cancelled run is ignored only when a later run of the same workflow on the same head sha succeeded — without it every pull request that was ever a draft is unmergeable at its ready sha), and the four delivery answers (repository goes public after a fork pull-request pass and a history scan; two pull requests, control plane first; sonnet writes, opus reviews, fable checks divergence; the agent writes the workflow diff under the unlock and the owner labels). Exploration on sonnet (three read-only scouts), design pass on opus; implementation-plan.md carries the exact diffs, the verification commands and the acceptance numbers
- 2026-09-09T14:30:00Z | intent.md | in-review -> in-review | claude | 4c6be66 | second session, on fable, reviewing the plan before execution: the repository went public first (owner, nothing else changed; gate green on this head at 12:36 UTC), so the fork pull-request pass is the first item of the control-plane pull request and the settings the flip skipped are Phase 0 clicks, each with its link; the history scan ran read-only, nothing found. Owner's revisions recorded as answered questions: opus writes and calls sonnet subagents, fable revises in full at each of five milestones; the skipped-run rule shrinks to one clause in _pr_runs and cancelled still refuses; the gate's `edited` trigger skips Bot senders (Sourcery's body edit doubled the gate run on 3abef21); a supervised item's merge run ends green `not-delegated` instead of red; the measure counts runs by event, since workflow_run wakes carry head_branch main; the owner retires run-queue-followups and moves .sdlc/active before any scripts/ edit; the `triage` label is still to be created; Sourcery uninstalled; failure e-mails off on the owner's side
- 2026-09-09T15:20:00Z | intent.md | in-review -> in-review | claude | 819c43b | two findings while checking the owner's manual steps. The approve tap cannot run from `main` yet: approve.yml writes to the ref chosen in "Use workflow from branch" and work/ci-budget/ exists only on this pull request's branch, so the order is merge #63, then tap on main with a blank slug (.sdlc/active already reads ci-budget); every earlier approval in this repository followed that order. And `main` carries no branch protection rule at all (branches API, protected: false), so Phase 1 step 3 is "create the rule" rather than "edit it", and the risk table's claim that a required status check is an independent second control for the skipped-run rule is corrected: today the merge script's require-checks and the owner's click are the controls. Verified as well: the pointer move landed (308f1a2, owner); Sourcery is uninstalled (no bot-triggered run since 12:37, and the doubled gate run stopped); the gate is green on this head in 36 s; run-queue-followups is not retired yet (intent approved, spec and plan delegated), which blocks nothing but leaves the item open in the index
- 2026-09-09T16:10:00Z | intent.md | in-review -> in-review | claude | 894e11c | review of the owner's manual steps, and the state main was left in. The approve run at 15:52 resolved its blank slug against the pull request's own branch, whose `.sdlc/active` still read run-queue-followups, so approve.py wrote nothing and the run refused to commit; with pull request 63 merged, main now carries `work/ci-budget/` and the pointer the tap needs. Commit 894e11c then set `work/run-queue-followups/intent.md` to superseded from the web editor without regenerating the two files that render that status, so `index-drift.sh` fails on main and on every branch cut from it, and it left that item's chain inconsistent: `spec.md` and `plan.md` are still delegated, which needs a human-approved intent behind them, and no ledger line records the supersede. Completing that retirement is not possible as the kit stands, because a superseded artifact needs a valid human approver and both artifacts carry approved-by claude, which `.sdlc/approvers.yaml` lists under never-approve; retiring any delegated item is therefore a defect of its own, and its own item after this one. Recommended to the owner: revert 894e11c, which restores the state where both indexes were already in sync. This commit carries the reviewer's one nit from pull request 63 (the "over 62" denominator now names its unit) and this line, nothing generated
- 2026-09-10T08:04:09Z | intent.md | in-review -> approved | luissiviero | 9811483
- 2026-09-11T09:00:00Z | spec.md | (none) -> in-review | claude | ca8dd32 | fourteen requirement rows from the intent outcomes and implementation-plan.md, each with its test; four areas of concern (the two widened merge-script paths, no branch protection, the control-plane edit, the fork route); written on fable in the session that ran the mock walk, since the guide's opus writer was not this session's model, with a sonnet explorer re-checking every cited line against main (seven had moved); reviewers to run on opus and sonnet before the tap
- 2026-09-11T15:40:00Z | spec.md | in-review -> in-review | claude | 58d7647 | two reviews before the tap, writer fable: compliance on sonnet (Important 2, Nits 2: four workflow-shape tests moved into test_check_workflow_permissions.py beside SdlcGate, README gate row is line 85, eval case named skill-opens-drafts, model deviation recorded as D7) and security on opus (Important 4, Nits 5: a cancelled run evicted from a concurrency group refused a ready sha forever, so R6 regains the narrow cancelled clause (C5); the not-delegated verdict placed before pull-request hid a locked-path refusal, so R7 keeps pull-request and locked-paths evaluated (C6); the comment route reads the head branch's .claude and CLAUDE.md, so the pin step restores them from base (C4, R3); the minutes measure counted skipped runs, so R10 excludes them and the fixture reads 5.0, 2.0; the local step contract, the fork pull_request invariant (D8), the owner applying triage, --workflow rejected, a narrower 2σ tool). Every finding verified against the code before the edit; all addressed
- 2026-09-11T15:50:00Z | PR #69 | draft -> in-review | claude | c7f6aeb | spec pull request ready for the owner: merge, then the tap on main (artifact spec.md, mode supervised, slug blank); writer fable, reviewers sonnet and opus
- 2026-09-11T15:51:35Z | spec.md | in-review -> approved | luissiviero | e2b221c
- 2026-09-11T16:00:00Z | plan.md | (none) -> in-review | claude | 4a8fb4f | thirty-four paths in the file list, thirteen steps, nine risks, every spec row mapped to its test. Two departures from implementation-plan.md, both recorded in the plan text for the owner to read at the tap: the two pull requests run sequentially on this session own branch reset from main between them, which is the one-code-pull-request-at-a-time control itself, rather than the two branch names the guide gives; and each milestone revision is a read-only fable subagent rather than a session model switch, which keeps one writer per item. Writer opus, reviewers sonnet, milestone revision fable
- 2026-09-11T16:25:00Z | plan.md | in-review -> in-review | claude | 9b4dea6 | two reviews on sonnet, both verified against the code before applying. Conformance (Important 2, Nits 3): evals README names a trigger R4 deletes and is not on the exempt list, so it joins the file list; R13 was proved only by check_okf.py, which reads front matter and links rather than prose, so step 5 and Proof gained the two grep content checks; the Risks section gained the exposure spec C2 records, that main has no branch protection so R6 and R7 widen the only control there is, and the control-plane unlock; R9 regained test_adopt.py. Security (Important 5, Nits 5): check_check_runs reads a different API list where cancelled is not accepted, so fixing _pr_runs alone would leave the deadlock R6 exists to clear, now in scope with an end to end case; the delegation verdict would have masked an agent self-signed grant, so it defers to the grant condition on a delegated intent status; the fork guard ordering claim was overstated and is corrected, since PR-A merges as one unit; the milestone revision now runs under the existing bounded-tools reviewer roles pinned to fable, which is what rule 8 requires. Writer opus, reviewers sonnet
- 2026-09-11T16:35:00Z | plan.md | in-review -> in-review | claude | acf5530 | M1 milestone revision on fable, under the plan-reviewer role, full pass over intent, spec and plan. Blocking finding taken: the check_check_runs clause the security round added contradicts approved spec R6, D2 and C1, and its premise was never verified, since that call passes no filter so GitHub default latest applies and the superseded row may never be returned; withdrawn, with the end to end case named as what would expose a real deadlock and the owner as who decides any widening. Six verifiable commands would have passed with the work absent or wrong and are now specific: the triage grep matched text already on line 89, the adopter render proved the cap and not the bullet, the drafts eval matched a string already present, the two added invariants had no test, step 2 cited a suite the workflow does not invoke, and the fixture literal is six decimals. Also corrected: the branch reuse and the fable subagent are departures from the approved spec D1 and D7, not only from the guide, and are the owner decision at this tap; R7 buys signal, not minutes, since the wake bills either colour and stays red off the active item; the six minute target is the floor with two assumptions; the fork guard adds one API call to pr-review, now named. Writer opus, reviewers sonnet, revision fable
- 2026-09-11T16:40:00Z | PR #70 | draft -> in-review | claude | 3955408 | plan pull request ready for the owner after three review rounds: conformance sonnet, security sonnet, M1 revision fable. Two spec departures and two overstated spec claims named in the pull request body for the tap decision
- 2026-09-11T16:45:00Z | plan.md | in-review -> in-review | claude | a484f63 | the automated reviewer on the ready pull request, Important 1 Nits 0, a memory-pass finding: six commands in this plan would have passed with the work absent, one shape caught six times, and rule 7 asks for a lesson in the same pull request. It cannot be this one: a knowledge or docs path in the diff leaves in-progress mode, and strict mode needs an approved plan, which is what this pull request is asking for. The lesson, its index line and the pointer fragment therefore join the file list and step 8, whose budget is now two rendered lines paid by re-flowing prose and measured on the adopter render. Answered on the pull request. Writer opus, reviewer claude bot on the ready head
- 2026-09-11T16:47:36Z | plan.md | in-review -> approved | luissiviero | 19b177b
- 2026-09-11T17:05:00Z | plan.md | approved -> approved | claude | 104c27d | PR-A implemented, steps 1 to 5 of the order of work. Tests first: 11 red on exactly the intended names, then green. R1 and R2 in sdlc-gate (ready_for_review, the draft and Bot-edit guard, per-pull-request concurrency, both triage steps behind the label), R3 in pr-review (concurrency, the head-repository guard gating the checkout, the pin step restoring the agent configuration from base on the comment route), R4 in agent-evals (nightly and dispatch only, hook-cases gone), R5 in delegated-merge (two workflows, with a test binding that list to the policy so neither can be edited alone), R6 and R7 in the merge script with 20 new cases, and R13 in seven documents. 867 unit tests green, VERIFY PASS, OKF 0 warnings. One deviation, logged in the plan: an end-to-end test pins the exact list of condition names, which R7 lengthens by one, so its expected list gains delegation; no test removed or weakened. Writer opus
- 2026-09-11T19:00:00Z | plan.md | approved -> approved | claude | 04d26ed | conformance review of PR-A on sonnet, Important 0 Nits 1: the spec R7 acceptance column names an end-to-end test that did not exist under that name, the equivalent living in DelegationCondition; added EndToEnd test_supervised_pull_request_ends_not_delegated, which asserts the stronger fact that the fixture recorded no mutating call at all, so a green not-delegated verdict provably merges nothing. The reviewer also re-ran every new test against the reverted source and reported about 28 failures, so none of them passes either way, which is the failure shape M1 found six of in the plan. Two earlier reviewer subagents were lost to a session interruption and never reported; relaunched. Writer opus, reviewer sonnet
- 2026-09-11T19:15:00Z | plan.md | approved -> approved | claude | 4d82d9a | security review of PR-A on sonnet, Important 2 Nits 2, both importants verified in the code and both real widenings this diff introduced. First: the superseded-run helper joined on the workflow name, which the head branch writes, so a diff adding a workflow file called sdlc-gate whose body always succeeds could launder the real gate cancelled row and the required check would read green for a commit the real gate never judged; before this rule that forgery bought nothing, since every matching run had to be green. Now keyed on workflow_id, with a run carrying none superseding nothing, and two tests. Second: the comment-route pin restored four paths but not docs/sdlc, while CLAUDE.md sends the agent to read docs/sdlc/README.md and the merge script ALWAYS_LOCKED floor already counts docs/sdlc/rules and templates as judging surface, so a head branch could hand the reviewer its own policy; docs/sdlc joins the restore. All three new tests proved to fail against the reverted source and pass with it. 212 tests in the three modules, workflow checks clean. Writer opus, reviewer sonnet
- 2026-09-11T19:30:00Z | plan.md | approved -> approved | claude | 441da35 | M2 milestone revision on fable over the whole PR-A diff, full pass. Six findings taken, all verified first. A structural defect of my own: the two new test classes sat after the if __name__ block, so running that file directly skipped six tests; the block moved to the end and both invocations now run 45. The plan Proof row promised an end-to-end evicted-attempt case that did not exist, so there are now two, one carrying a cancelled workflow run through the whole pipeline to a merge and one pinning the check_check_runs residual as a fail-closed refusal with no mutating call. Two workflow comments claimed the head sha never carries a cancelled run, which contradicts spec C5 and the docstring of the script in the same diff; corrected. The comment-route pin was overlay-only, so a head that ADDED a skill or agent file kept it in front of the reviewer; now git restore --no-overlay. A mode misspelled Delegated answered a green not-delegated and swallowed the grant reason; it defers to the grant now, with a test. And a correction to this ledger: the earlier line claimed every new test fails against the reverted source; M2 showed about eleven are boundary pins that pass against main, which is legitimate, but one, the cool-off skipped-stamp oracle, could not tell no stamp from a stamp too recent and is now sharpened both ways. 873 unit tests green. Writer opus, revision fable
