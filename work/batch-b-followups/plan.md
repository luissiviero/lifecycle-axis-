---
type: sdlc/plan
id: batch-b-followups
title: Close the three leftovers Batch B surfaced
description: "Files, order, proof and risks for the trust step, the never-approve entry with its test and docs, the superseded-predecessor rule and the handoff update."
stage: build
status: approved
kind: feature
reads: spec.md
approved-by: luissiviero
approved-on: 2026-09-05
risk-class: low
record:
resource: https://github.com/luissiviero/lifecycle-axis-/pull/38
tags: [followups, sdlc-gate, adopt, chain-check, control-plane]
timestamp: 2026-09-05T05:25:00Z
---
# Plan: close the three leftovers Batch B surfaced (from intent.md 2026-09-05)

## Files that change
- .github/workflows/sdlc-gate.yml — one trust step before the triage step (spec D1; control plane)
- .sdlc/approvers.yaml — line 26 gains `"<your-github-handle>"` under `never-approve`; edited by the owner from the web editor, never by the session (D2; control plane, human-only file)
- scripts/test_adopt.py — `test_handle_rewritten` asserts the placeholder holds no role and the copied file carries the entry (D2)
- docs/sdlc/github-setup.md — step 1 sentence (D2)
- knowledge/decisions/adopt-script.md — the C1 bullet: entry exists (D2)
- scripts/check_artifact_chain.py — one comparison and its message in the stage-order rule (D3)
- scripts/test_check_artifact_chain.py — `InProgressChain::test_fully_superseded_chain_passes` (D3)
- docs/sdlc/handoff/HANDOFF.md — task state and work-item order lines (D4)
- .sdlc/active — set to `batch-b-followups` by the session after the plan is approved
- work/batch-b-followups/intent.md — new; this item
- work/batch-b-followups/spec.md — new; this item
- work/batch-b-followups/plan.md — new; this item
- work/batch-b-followups/log.md — gate ledger, lines appended
- work/batch-b-followups/index.md — generated by `gen_index.py`
- work/index.md — regenerated
- work/sdlc-kit-phase-1/index.md — regenerated by PR #38, which this branch is based on; no change of its own
- work/sdlc-kit-phase-1/log.md — final newline from PR #38; no change of its own

## Release-gated
(none)

## Order of work (each step independently verifiable)
1. Preconditions: `.sdlc/active` reads `batch-b-followups`; `python3 scripts/run_tests.py` green on the base (count noted); `python3 scripts/check_artifact_chain.py --base 2190c0c --slug sdlc-kit-phase-1` ends `CHAIN: FAIL` with the two stage-order lines (the defect reproduced); `git log -1 --format=%h -- .claude/skills` prints `60ab0f5`. A difference is recorded under Deviations before any edit.
2. Test first: `test_fully_superseded_chain_passes` red; `test_handle_rewritten` flipped and red (the entry is not in the file yet).
   This item is `kind: feature`, not `kind: fix`, although R-2 and R-3 correct defects: `kind: fix` makes
   `protect-tests.sh` refuse every edit to an existing test file, and R-2 must flip an assertion inside
   `scripts/test_adopt.py::test_handle_rewritten`. The test-first discipline the `fix` kind would enforce is
   this step instead, and each red-then-green transition is recorded under Deviations.
3. `check_artifact_chain.py` comparison and message (D3); `test_check_artifact_chain.py` green; the R-3 oracle prints `CHAIN: PASS`.
4. `sdlc-gate.yml` trust step (D1); `scripts/checks/workflow-yaml.sh` and `workflow-permissions.sh` pass; additions-only oracle prints `0`.
5. Owner: edit `.sdlc/approvers.yaml` line 26 on this branch from the web editor (link and exact text in the session's message); the session pulls and continues.
6. `python3 scripts/approvers.py --has-role tech-lead '<your-github-handle>'` exits 1; `test_adopt.py` green; `github-setup.md` and `adopt-script.md` sentences; the R-2 greps.
7. `HANDOFF.md` (D4); the R-4 greps; `python3 scripts/check_okf.py` ends `0 warnings`.
8. `python3 scripts/gen_index.py`; `scripts/verify.sh`; chain check with `--slug batch-b-followups`; `scripts/run_evals.sh`; test count grows by one; ledger line `PR #<n> | draft -> in-review`; PR body with the last lines and the request for `control-plane-approved` (`sdlc-gate.yml`, `.sdlc/active`; the owner's own `approvers.yaml` commit is human-authored).

## Risks
- Risk: the owner's `approvers.yaml` edit and the session's test land in the wrong order → mitigation: step 5 precedes step 6; the session does not push step 6 until the edit is on the branch.
- Risk: the widened comparison lets a chain with a superseded intent and a `draft` spec pass → mitigation: no; the rule still requires the immediate predecessor to be approved or superseded, and a draft spec's own predecessor check is unchanged; D-b records the accepted staged-retirement case.
- Risk: the trust step's heredoc indentation breaks the YAML → mitigation: copied verbatim from `agent-evals.yml`; `workflow-yaml.sh` parses it with PyYAML in step 4.
- What this could break: which PRs the chain check accepts (wider by one status); the adopter's first hour (stricter by design).
- Options considered and not taken: three separate items (intent Q2); a shared trust script under `scripts/` (D-a); asking the owner to edit the workflow by hand (a 14-line indented paste from a phone).

## Proof
- `scripts/verify.sh` green: last line `VERIFY: PASS (<sha>)`
- Spec rows → tests: R-1 → `grep -c hasTrustDialogAccepted` = 1, additions-only = 0, both workflow checks; R-2 → `approvers.py --has-role` exit 1, `test_adopt.py` green, the two greps; R-3 → `CHAIN: PASS` on `--slug sdlc-kit-phase-1`, `test_fully_superseded_chain_passes`, the three existing supersession tests; R-4 → the two greps; R-5 → `VERIFY: PASS`, `CHAIN: PASS`, `EVALS: N pass, 0 fail, N skipped`, `OKF: N docs, 0 warnings`
- Manual / browser / screenshot / eval: the next failing `sdlc-gate` run's triage log has no "workspace has not been trusted" line.

## Rollback
- Revert the PR's commits and the owner's `approvers.yaml` commit; the three defects return as documented in the intent.

## Deviations log (append during implementation; same commit as the deviation)
- 2026-09-05 — step 1: `git log -1 --format=%h -- .claude/skills` prints `c420e3c`, not `60ab0f5`. WI-11 changed `.claude/skills/sdlc-incident/SKILL.md` between the drafting of this spec and its approval; the `skills-version` in `spec.md` records the drafting value and is left as history.
- 2026-09-05 — step 1: the reproduction command `check_artifact_chain.py --base 2190c0c --slug sdlc-kit-phase-1` no longer reproduces the defect on this branch. The branch's diff spans two work items, so `in_progress` is false and the check reports the full-mode rule instead (`status is 'superseded', must be 'approved'`). That rule is correct and stays: a retired plan must not authorize a code diff. The defect R-3 fixes lives only in the in-progress stage-order rule.
- 2026-09-05 — step 7 found a defect in the same file and fixed it in the same edit: `docs/sdlc/handoff/HANDOFF.md` carried a raw NUL byte at offset 7977, so git and grep treated the document as binary. The sentence containing it documents the very bug that produced it ("The Write tool turns a `\0` escape inside a heredoc-free file into a raw NUL byte"), written with the Write tool. The two-character escape is restored and `file` now reports UTF-8 text. Neither `check_okf.py` nor `check_front_matter.py` catches this: both read with `errors="replace"`. Not widened further; a check for control bytes in tracked text is a candidate for its own item.
- 2026-09-05 — step 3: the R-3 field oracle of record is therefore PR #38's exact head, checked in a detached worktree with the fixed script: `check_artifact_chain.py --base 2190c0c --slug sdlc-kit-phase-1` on `ef2db87` goes from `CHAIN: FAIL` (two stage-order lines) to `CHAIN: PASS`, in-progress mode. `test_fully_superseded_chain_passes` is the committed reproduction: red before the fix with the same message, green after.
